# Gable — decision log

Every design decision, with the reason it was made. **Append-only.** When a
decision reverses, add a row saying so and why; never edit an old row away.
The reversals are the reason the next agent does not repeat the week that
proved them.

This lived in `ARCHITECTURE.md` §9 until 2026-08-13, when that file reached the
800-line ceiling `tests/test_repo_standards.py` enforces. The log is the part
that grows forever, so it moved here.

| Date | Decision | Reason |
|---|---|---|
| 2026-08-10 | Bulk Create over Connect API autofill | Autofill needs Enterprise; Teams is the plan in hand |
| 2026-08-10 | Socket Mode over HTTP events | No TLS/domain/ports on a $4 droplet |
| 2026-08-10 | Poll the Sheet, don't webhook it | Follows from Socket Mode; fine at this volume |
| 2026-08-10 | Google Sheet as datastore | Human-inspectable; volume doesn't justify a DB |
| 2026-08-10 | Photo policy is configuration, not code | Chase hasn't decided; don't decide for him |
| 2026-08-10 | Phase 1 Python, Phase 2 TypeScript | Apps SDK is TS; prove the pipeline before adding a stack |
| 2026-08-10 | `slack-manifest.yml` moved to `slack/manifest.yml` | CLAUDE.md §6 specifies that path; README's file table said otherwise. Followed §6 and corrected the README. |
| 2026-08-10 | ruff line-length 100, not the default 88 | Annotated signatures plus the documentation-URL comments §5.3 requires do not fit in 88 without breaks that hurt readability. Prose files stay at 80. |
| 2026-08-10 | `mypy python_version = "3.11"` while developing on 3.13 | CLAUDE.md §9 sets 3.11 as the floor and the droplet's distro Python decides the ceiling. Pinning the check to the floor stops 3.12+ syntax reaching the server. |
| 2026-08-10 | Config fails only on *unsatisfiable* combinations; everything else degrades safely | A first pass rejected `GABLE_IMAGE_PROVIDER=openai` with a blank key — which is what `.env.example` ships, so the documented defaults would not boot. Phase 1 needs no image model: a missing provider means the cascade ends at "ask Carmen," which is safe. Only `generate_freely` with no usable provider is genuinely unsatisfiable and still refuses to start. |
| 2026-08-10 | Photo policy is authoritative; `GABLE_PHOTO_ENHANCE` is subordinate | `no_ai` disables enhancement whatever the flag says, instead of erroring and making the operator edit two variables to express one intent. Exposed as `Settings.enhancement_enabled`. |
| 2026-08-10 | `LOG_REDACT_SECRETS=false` is rejected outright | CLAUDE.md §3 makes redaction a mechanism, not a preference. A typo in `.env` must not be able to disarm the only thing standing between a token and journald. |
| 2026-08-10 | **Spike A FAILED — an uploaded xlsx/CSV cannot carry an image column** | Observed live: uploaded columns are all typed `T` (text), while the manual table's "Add image" produces a distinct image-typed column. §2.4 and §4.7 assumed the photo travels in the file; it cannot. **No redesign made — awaiting Chase.** See `spikes/SPIKE_A_RESULT.md`. |
| 2026-08-10 | **The live form is not the form this document describes** | The real sheet is "Social Media and Marketing Request Form (Responses)": Timestamp, Email Address, Name of Agent, Service Guidelines Acknowledgment, request type, property address, postcard category, Upload photos, video assets. No Price, Description, Beds/Baths/Sq ft, or agent phone — and no `Agents` or `Runs` tab exists. §3.1's column table is superseded. **Awaiting Chase on scope.** |
| 2026-08-10 | **REVERSES row 1: Google Slides replaces Canva entirely** | Spike A killed Bulk Create-from-file, autofill needs Enterprise, and the data-connector app is gated on an unverified review question plus a second language. Slides' `replaceAllShapesWithImage` places the photo from a public HTTPS URL — the exact capability Spike A proved a Canva upload lacks — on the service account already needed for the Sheet. Bulk Create also has no API to drive, so it always needed a human at a keyboard. `src/gable/canva/` deleted; `src/gable/slides/` added. §1, §2.1, §4.5 and §4.7 rewritten. |
| 2026-08-10 | Renderer is pure; a separate `slides/client.py` holds all I/O | Made the entire fill behaviour unit-testable (36 tests) before a Google credential existed — which is why the renderer is finished while the credential is still blocking. |
| 2026-08-10 | Drive must be a **shared drive**, rejected at startup | Service accounts have a 0 GB storage quota, so a file created outside a shared drive fails with `StorageQuotaExceeded` on the first render. Better to refuse at boot than to discover it on a real listing. |
| 2026-08-10 | "Enhancement" renamed to **reprocessing**; `ImageProvider` enum dropped | Reprocessing a real photo to fit the frame is the common case and the reason an image key exists; generation invents a subject. One OpenAI image key plus one Anthropic key replaced the provider enum — `Settings.images_available` is the single question that matters. Supersedes the `GABLE_PHOTO_ENHANCE` row above. |
| 2026-08-10 | Output canvas is Instagram Post 4:5, 1080 × 1350 | Confirmed on export from the Corner House template library. The product is social posts, not only printed flyers. |
| 2026-08-10 | The real droplet is 1 vCPU / **1 GB** ($6/mo), not the $4 / 512MB tier | Verified live: 961 MB RAM, 1 GB swap active, Python 3.12.3. `CLAUDE.md` §9 corrected. Size against 1 GB. |
| 2026-08-10 | `mypy` now covers `tools/` as well as `src` and `tests` | `tools/check_connections.py` was ~310 lines of real annotated code sitting outside the strict gate. Adding it surfaced one genuine `no-untyped-call` on `google-auth`, narrowed to a single line rather than a module exemption. |
| 2026-08-10 | **Gable is a conversational agent, not a pipeline** | §1 rewritten. It runs until it needs a human, then asks — for the hero image, for a phone number the form never collected, for confirmation it understood. AI-centric (the model chooses tools and inspects its own output) and human-centric (asks rather than guesses) are both requirements, not adjectives. New §4A covers confirmation, progress messages and tool use. |
| 2026-08-10 | Polling is schedule-aware: **2 min Mon–Fri 07:00–17:00 US Central, 10 min otherwise** | Requests arrive during the working day; polling at the same rate at 3am burns Sheets quota to find nothing. Evaluated against a timezone-aware Central clock, never droplet-local UTC — that variant works all winter and breaks in March. |
| 2026-08-10 | `Agents` tab renamed **`Salespeople`**, joined on first/last/email | Matches how Chase describes it and how a human corrects it. Gains `phone`, which the form does not collect and which belongs to the agent rather than the listing — asked once ever, not once per listing. |
| 2026-08-10 | Agent headshot is a dynamic image field, not a template variant | The Corner House library bakes an agent photo into many designs. One template per agent per request type is ~40 × N and unmaintainable; a second `replaceAllShapesWithImage` target keeps it at ~40 total. |
| 2026-08-10 | Phone renders as **`(818) 259-7432`**, not E.164 | Display format, not storage format. E.164 is right for dialling an API and wrong for print — nobody puts a plus sign on a flyer. The output is read by a human. |
| 2026-08-10 | Image model pinned to **`gpt-image-2`** via `GABLE_IMAGE_MODEL` | Verified available to the key against `/v1/models` and the images endpoint. Fitting a phone photo to a 1080×1350 frame is the hardest task in the product and gets the newest model. An earlier report claiming only `gpt-image-1` was a display bug in the checker — it sorted ascending, so `gpt-image-1` appeared newest. |
| 2026-08-10 | **Gable inspects its own render before delivering** (§4.7b) | The failure mode is silent: every API call can succeed while the roofline is cropped off. The API cannot see that; a vision pass over the rendered thumbnail can. This is why the Anthropic key matters as much as the image key — one model makes the picture fit, the other checks whether it did. |
| 2026-08-10 | Asking for missing fields is a first-class stage (§4.3b) | The form collects no price, phone or beds/baths, and its address column is empty on every observed row. "A required field is absent" is the normal path, so it gets a named status (`needs_info`), a specific question, and a pause — not a failure. |
| 2026-08-10 | **Corrects the row above:** the tab is `Sales_People`, not `Salespeople` | Read live through the service account once Google access existed. The earlier row named a tab that does not exist — the guess was close enough to look right and wrong enough to return nothing. Config now carries the literal live name. |
| 2026-08-10 | `Sales_People` headers are on **row 2**; row 1 is blank | Observed, not assumed. Any reader that treats `A1` as the header row matches nothing and reports "no agents" on a tab that has agents. The header row is found by content. |
| 2026-08-10 | Every `Sales_People` join key is **trimmed before comparison** | The one live row holds `"Lolo "` with a trailing space. Untrimmed, Lolo never matches herself and every one of her listings pauses as an unknown agent. |
| 2026-08-10 | Google service account has **no project IAM roles** | All of its access comes from two Drive shares — the intake Sheet as Editor, the `Gable` shared drive as Contributor. Its blast radius is exactly those two things and nothing else in the `monarchconnected.com` org. |
| 2026-08-10 | Contributor, not Content manager, on the shared drive | Verified capability: `canDelete: false, canTrash: true`. Gable never permanently deletes (CLAUDE.md §11), so the weaker grant is sufficient and is the one to keep. Cleanup uses `trashed: true`, never `files.delete`. |
| 2026-08-10 | The whole Slides render path is **verified live**, not assumed | Create in the shared drive → `batchUpdate` → `replaceAllText` returning `occurrencesChanged: 1` → `getThumbnail` at 1600px. The 0-quota trap of §2.4 was specifically tested and does not fire inside the shared drive. |
| 2026-08-10 | **Hero photos come from Carmen in Slack, not from the form.** | Chase's call. The form's photo columns hold Drive links in `aj@cornerhouserealty.com`'s Drive that neither Gable nor Chase can read (404), and 34 of 40 filled cells hold 3-5 URLs rather than one. Sourcing from Slack removes an access dependency on a third party and an ambiguity about which photo is the hero. It also collapses the CLAUDE.md §8 cascade to its step 5. |
| 2026-08-10 | **Slides will not fetch an image from Google Drive. Verified.** | Tested three URL forms — `uc?export=view`, `uc?export=download`, `thumbnail?id=` — against a file the service account had made world-readable and which returned valid PNG bytes to an anonymous request. All three rejected: *"There was a problem retrieving the image."* A public `picsum.photos` JPEG in the same batch was accepted with `occurrencesChanged: 1`, so the harness was sound. This was previously an assumption in §4.5; it is now a fact, and it means a separate public host is not optional. |
| 2026-08-10 | The service account **can** publish a Drive file (`role: reader, type: anyone`) | Worth recording even though it does not help here: the permission call succeeds and the file becomes anonymously fetchable. Drive is a usable public host for anything *other* than a Slides image fetch. |
| 2026-08-11 | **Reverses the Spaces row:** photos are hosted on the droplet over plain `http://` | Slides was assumed to require https. It does not — verified live, with a picsum control in the same batch. nginx on the existing droplet serves them, which removes a vendor, a credential and a monthly cost. Spaces stays in config for the day one box is not enough. |
| 2026-08-11 | Fitting a photo to the frame is **Pillow, not a model** (`photos/fit.py`) | Cropping and resizing to an aspect ratio is deterministic and free. A model is only needed to invent pixels — upscaling a source too small for the frame. This reverses §4.5b's assignment of the job to `photos/enhance.py` and `gpt-image-2`, and it takes a paid call off the common path entirely. |
| 2026-08-11 | Conversation and vision run on **`gpt-5-mini`**, images on **`gpt-image-1-mini`** | Priced from each vendor's own page. gpt-5-mini is 4x under Haiku 4.5 and 8x under Sonnet 5 on input, and this is the highest-volume path. Anthropic stays configured as the escalation if tool-picking proves weak. Estimated ~$2/month against ~$16 on Opus 5 plus gpt-image-2. |
| 2026-08-11 | The Slack house style is **enforced in code**, not documented (`slackapp/style.py`) | A guide nobody can run drifts, and this one already had: every card in `blocks.py` carried emoji, two carried red code spans, and raw HttpError text reached the channel. `violations()` now runs before a message is posted, so a breach cannot reach Carmen. |
| 2026-08-11 | `action_id` must be **unique within a Slack message** | The unknown-agent card gave every template button the same id, so Slack answered `invalid_blocks` and the card could never be posted at all. Found by posting it for real. Repeated buttons now carry a numeric suffix and handlers route on `dispatch_key`. |
| 2026-08-11 | **A local SQLite database**, not the Sheet, holds what Gable derives | The Sheet has no types, no constraints, no transactions, and no way to ask "have I built this?" without reading every row. It stays the source of truth for what agents submitted and is never written back to. SQLite rather than Postgres because a hundred submissions a month on a 1 GB droplet does not justify a second daemon to run, back up and patch. |
| 2026-08-11 | The poller **refuses to start** until the backfill is adopted | 99 historical rows are on the live sheet. A first poll treating them as new would build 99 flyers and spend real money in about eight minutes. `tools/adopt_backfill.py` marks them as history and builds none; until it has run, `new_submissions()` returns nothing and `Poller.ready()` says exactly what to run. |
| 2026-08-11 | A long-running loop, **not cron** | Cron cannot express two rates without two entries that disagree at the boundary, and it hands you a fresh process, a fresh Slack connection and a cold cache each time. A loop under systemd restarts on failure and asks the schedule how long to sleep. |
| 2026-08-11 | Beds, baths and square footage are **researched, never asked** | Chase's rule: a template must never ship with a blank that is public information. Verified live — 3 Nob Hill Park Dr returned 4 bed, 2.5 bath, 2,915 sqft, $525,000 at 0.95 confidence. Zillow is excluded by name because scraping it violates their terms (CLAUDE.md §8). |
| 2026-08-11 | The **second agent arrives as prose in column N**, not as a missing form field | Row 84 reads "Listed by: Stacey Abbott. Hosted by: Jason Vetter", and slide 28 is a two-agent Open House design carrying exactly those names. The submitter is the HOSTING agent there, so assuming the submitter holds the listing renders perfectly and is false. |
| 2026-08-11 | The orchestrator **decides but never performs** | Every step returns what happened and what should happen next; the caller does the I/O. That is what makes the whole sequence testable without Google, Slack or a paid call, and what stops a wrong decision becoming a wrong action. |

Append to this table. Do not rewrite history — if a decision reverses, add a new row explaining why. `CLAUDE.md` §2.7 makes this mandatory rather than polite.

| 2026-08-11 | A design's field set is a **per-template manifest**, not one global column list | Two rendered flyers were reviewed and their field sets differed. Treating them as one shipped a flyer carrying the literal words "Phone" and "Website". `slides/manifest.py` gives each design its own required fields and measured character budgets; a missing required field is a hard stop, not a blank. |
| 2026-08-11 | **Every image URL is proven before it is emitted**, not trusted because it ends in .jpg | One flyer put the template's own background illustration in the headshot frame because nothing looked at what was behind the link. `photos/verify.py` fetches, checks content type, dimensions and aspect band against the slot. Verified live: a 1080x1350 image is rejected for a landscape slot and accepted for a square one. |
| 2026-08-11 | The hero slot is **portrait**, not landscape | The Corner House deck is Instagram 4:5 throughout. The slot was briefly typed as landscape, which would have rejected correctly-shaped photos. |
| 2026-08-11 | Addresses are canonicalised to `street, city, ST ZIP`, and a **missing ZIP is never invented** | A flyer shipped reading "3 Nob Hill Park Dr, Reisterstown, MD" with no ZIP. `normalise_address()` reshapes what is there and refuses to guess what is not; `validate()` stops the run instead. |
| 2026-08-11 | **Template defects are logged for Carmen, never worked around in code** | Chase's instruction on reviewing two flyers: the "approch" typo, mixed typefaces, the low-contrast logo and panel misalignments belong to the design, not the pipeline. `TEMPLATE_ISSUES.md` records them. A code workaround hides the defect from the person who can fix it and breaks on the next re-export. |
| 2026-08-11 | Socket Mode connects in the background; the Sheet poller owns the main thread | `Poller.run_forever` installs signal handlers and must run on the main thread. `slackapp.runtime` opens Socket Mode with its non-blocking `connect`, then calls the generic lifecycle in `runtime.py`. Slack event handlers use separate database connections rather than sharing the poller's connection. |
| 2026-08-11 | Paused and review states suppress polling, and a submission gets at most three fresh attempts | Re-polling `needs_photo` repeated paid research and Slack questions indefinitely. Those states now resume their existing run only after a human response; `start_run` is the hard attempt ceiling. |
| 2026-08-11 | Slack hero uploads resume the **same paused run** and publish locally on the droplet | A `file_share` is accepted only in the configured channel and originating listing thread, with exactly one image. It is downloaded from a Slack-owned host with bot authorization, capped at 25 MB, fitted to 1080 by 1350, atomically written to nginx's directory, verified anonymously, and passed to `Runner.resume`; no new retry is opened. The app needs Chase to approve and reinstall the new `files:read` scope before live testing. |
| 2026-08-11 | Conversational edits resolve the Slack thread to one Slides output and report success only after Google confirms | The earlier app announced tools it never executed. Font size, colour, field correction, photo resize, element movement, and status now run through pure request builders. Zero or multiple matching elements is an explicit refusal rather than a guessed object id. |
| 2026-08-11 | Template choice reads **all notes fields** against explicit per-template purpose metadata | Request type chooses only the broad category. `slides/selection.py` combines post details, open-house details, extra notes, transaction side, and final notes to distinguish one or two agents, one or two dates, and calls to action. Functional mismatches are hard filters; a genuine tie or missing exact Drive file becomes `needs_template`. |
| 2026-08-11 | The live Slack manifest is JSON at `slack/manifest.json` | This supersedes the earlier path decision recorded above. The installed artifact and current repository file are JSON; setup documentation now names what actually exists. |
| 2026-08-11 | Hero placement uses an **explicit per-template raster-art object id**, never a largest-shape heuristic | A read-only inspection of the live imported files showed no ordinary image elements: photos and artwork arrive as shapes, and the largest text-free object can instead be a white panel or overlay. Three measured manifests name their exact removable hero layer. The 1080 by 1350 photo is inserted at full-slide bounds behind the surviving masks, which centres it without letterboxing. An unmeasured template or changed object id stops for review without sending a deletion request; the other 42 remain pending in `TEMPLATE_CERTIFICATION.md`. |
| 2026-08-11 | **Reverses the weekday-only polling window:** busy polling runs every day from 07:00 to 21:00 Central | Chase specified 7 AM Central through 7 PM Pacific, including weekends. Those endpoints are 07:00–21:00 Central because Pacific is two hours behind, and 18 of the 99 historical submissions arrived on weekends. `pipeline/schedule.py` and its DST tests already implement this; the earlier documentation was stale. |
| 2026-08-11 | Firecrawl, conversation, and visual inspection share one **hard $50 spend guard** | `spend.guarded_call` checks the cumulative SQLite ledger before the vendor, reserves a deliberately conservative per-call estimate, and records it even when a request fails after acceptance. Crossing the ceiling prevents the call. This makes the guard fail safe when exact token usage is unavailable and stops all currently connected paid paths through one mechanism. |
| 2026-08-11 | A small supplied hero photo is **upscaled automatically**, never rejected for resolution alone | Up to 2x stays on Pillow. Beyond that, one policy-gated GPT Image 2 edit restores resolution from the exact fitted composition, then a composition-distance and seam gate decide whether the derivative is faithful enough. The original Slack upload remains untouched, `ai_enhanced` is recorded only when the edit is used, a failed edit falls back to the original pixels, and the call shares both the one-image-call limit and $50 ceiling. |
| 2026-08-11 | `files:read` is installed and the private Slack download path is **verified live** | A real thread upload reached Gable's dimension check at 10:24, which requires successful `files_info` metadata and bot-authorized download. This supersedes the earlier waiting-on-reinstall status. The new AI upscale and resulting flyer remain unverified live until `e09bb27` is deployed in a watched run. |
| 2026-08-11 | The automatic upscale is deployed; publishing reasserts an **unprivileged writable photo root on every deploy** | A watched 10:51 upload reached GPT Image successfully, then the seam gate rejected the derivative and the original-photo fallback continued. Publishing exposed `/var/www/gable-photos` as root-owned even though systemd allowed the path. The directory is now `gable:gable`, and `make deploy` idempotently reasserts that owner and mode before restarting. This supersedes only the earlier row's deployment status; a completed live flyer and visual certification are still pending. |
| 2026-08-12 | The thinking indicator is a **posted, animated, then deleted message**, not an edited placeholder and not Slack's own thread status | Two designs were tried and both failed against what was asked for: an indicator that comes into the thread, runs, and goes away. A placeholder edited into the answer never goes away — it becomes the reply — and it strands permanently if the work raises, leaving a progress claim above a job that died. `assistant.threads.setStatus` looked correct and is not: it accepts `chat:write` and returns `ok`, but held open for twenty seconds on a live channel thread it rendered nothing, because that surface paints only inside an assistant-pane container. So `status.py` posts a real message, cycles its text every 1.5s, and deletes it — after the answer is posted, so no silent gap opens at the moment the wait ends. It runs on a background thread and swallows every error: a broken indicator must never affect the reply. Supersedes the update-in-place contract in §4A.2 and `AGENTS.md` §2.8. |
| 2026-08-12 | Responses columns are located by **header text**, and a tab with no recognisable header is refused | Fixed positions are only ever true of one tab. `Testing_1` splits the agent's name into `First Name` and `Second Name`, shifting every column from D rightward by one and heading row 2 under a blank row; read positionally its row 78 yields the service-guidelines paragraph as the request type, "Instagram Story" as the property address, and no price — all of which look like data downstream. `intake.columns_from_header` maps by name, `repository.find_header` locates the header row, and `maps_a_response_row` refuses a tab that names none of the email, request type and address rather than guessing. Also corrects §3.1, which described nine columns from a pre-API browser reading, and removes the `Templates` and `Runs` tab designs that D3 replaced with SQLite. |
| 2026-08-12 | The picker takes the best design **that has been imported**, unless the submission named the missing one | Only the top-ranked candidate was ever looked for in Drive, so Just Sold — which has eleven eligible designs and one imported — reported having none filed at all, in front of a customer. `rank` already drops functional mismatches rather than demoting them, so everything it returns is usable and the order is preference; an absent candidate is now skipped and the next taken, with the fallback logged. This does **not** reverse the earlier "missing exact Drive file becomes needs_template" row, it narrows it: a design that won on a cue was explicitly asked for in the notes, and substituting another answers a different question than the agent asked, so a missing cue-matched design still stops and asks. |
| 2026-08-12 | **Reverses the posted-message indicator:** every user response uses Slack's native purple thread status | Chase identified the posted animation as a regression from the native purple treatment that had visibly worked. Slack's current method reference and March 2026 scope update explicitly support channel apps through `chat:write`, including auto-opening the reply thread. The prior single live call that returned `ok` without visible output was not enough evidence to remove the product behavior. Mentions, follow-ups, edits, and photo work now share one timed status; after six seconds it reports the actual stage, and every exit clears it. |
| 2026-08-12 | **Template choice is a naming rule, not a selector.** One folder; the file's name is the form's request type | Chase set this with Carmen directly, and it replaces the scored catalogue. Templates live only in `Templates / Generic Templates`, and each is named exactly what the form calls that request type — `Sold` on the form uses a file called `Sold`. The picker matches on that name, tolerating only case and stray spacing, and refuses on a duplicate name rather than choosing. Two things drove it: Carmen maintains the designs, and a convention she can verify by looking at a folder is one she can keep correct, whereas a ranking that reads her notes is not; and the previous lookup found any presentation in the drive carrying a `gable_role` property regardless of folder, so seven of the eleven it offered were filed inside Kelsey Mahon's own folder and any agent's listing could have been rendered onto another agent's design. The obsolete catalogue, ranker, purpose resolver, and agent-override routing modules have been removed rather than left as a second unwired selection system. |
| 2026-08-12 | The form splits the agent's name into `First Name` and `Second Name` | Chase's request to the customer, so the sheet is easier to parse than a single `Name of Agent` free-text field. Already supported: `intake.columns_from_header` reads either shape, and joins the pair when there is no single name column. |
| 2026-08-12 | The roster moves out of the workbook tab and into the drive, and a **headshot is republished, never linked from Drive** | The `Sales_People` tab was deleted, so every run died on `Unable to parse range`. Contacts now come from `Agents Contact Information / Sales_Agents_Contact_Information.xlsx` and faces from `Head Shots`, each matched by name. The face cannot be handed to Slides as a Drive URL (§4.3 item 1), so it is downloaded with the service account and published through nginx like a hero photo; `publish_local` is content-addressed so each is written once. `agents/contacts.py` mirrors the workbook into the existing `salespeople` table, which left `find_salesperson` and its callers untouched. |
| 2026-08-12 | **A missing price no longer stops a sold post** — the flyer is built, the link posted, and the price offered afterwards | Chase's rule. A flyer with a photo, an agent, an address and a design should not wait on a number that can be typed into the thread in seconds. The offer is made **only when the chosen design has a price field**: the live `Sold` design carries the address and the agent card and no price at all, and offering to add one there promises something that cannot be done. |
| 2026-08-12 | Deleted every module unreachable from the entry points — about 3,000 lines of `src` and 1,600 of tests | Reachability from `slackapp.runtime`, `cli` and `tools/run_row` found thirteen orphans: `measure`, `registry`, `renderer`, `routing`, `catalog`, `blocks`, `handlers`, `quality`, `resolver`, `sources`, `verify`, `models` and `normalize` (whose one live function became `listings/headers.py`). Some were days old and never wired; keeping them meant the next agent reading a routing rule that routes nothing. `boto3` went with them, since Spaces was abandoned for nginx, and `openpyxl` became an explicit dependency. |
| 2026-08-12 | **Ordinary Slack replies are gated by thread-root ownership** | Gable answered Chase's keyword selection inside a Monarch Website Watcher thread because the handler treated every `thread_ts` as Gable context. It now answers without a repeated mention only when Gable authored the root or the root originally mentioned Gable. Direct mentions still work in foreign threads, but do not transfer ownership. Root lookup failures stay silent, and the bounded cache prevents one Slack read per later reply. |
| 2026-08-12 | The hero photo is cropped to the **frame's** shape, not the slide's canvas | `createImage` fits an image inside the box it is given rather than filling it. The upload is fitted to the slide's 1080x1350 canvas when it arrives in Slack — aspect 0.80 — and the `Sold` design's photo area is the full width by 37% of the height, aspect 2.14. The photo was therefore drawn at the band's height and centred: a narrow column of photograph with the layout showing either side and the design's angled mask exposed. It is now recropped to the measured frame's pixel size (1078 by 504 here) before placement. This affected every design whose photo area is not 4:5, which is nearly all of them. |
| 2026-08-12 | **A question means Gable is waiting.** Anything it is not waiting for is a statement, said once, at the end | A non-blocking advisory — "the address is 45 characters and this design fits about 42, shall I shorten it?" — was posted mid-build and then ignored by Gable itself, which shrank the text and delivered. Four messages reached the thread for one flyer. Advisories are now collected and folded into a single closing message with the link, phrased as what was done; the photo handoff stays silent when the run has already spoken; and the native waiting state remains through the question or outcome post, then clears. Only the states that genuinely stop — no photo, no design, an unusable address, a contradiction — ask anything. |
| 2026-08-12 | A new submission is **two messages**: the listing announced, the photo requested as a reply | The combined one-liner started no thread, and `PhotoHandoff` matches an upload to a run by the thread it arrives in — so there was nowhere valid to put the photo and the run would have waited forever. |
| 2026-08-12 | **Current-source geometry preflight precedes every copy; GPT-5.6 Sol is a fail-closed rendered gate** | Google already exposes the editable object's dimensions and transform, which is more trustworthy than inferring them from pixels. Preflight now measures this listing's actual values and supplied photo before mutation. The rendered thumbnail still catches masks, overlaps and crops that rectangles cannot; original-detail Responses output must be complete, strict, confident and positive or delivery stops. |
| 2026-08-12 | New files in `Generic Templates` receive proactive structure, capacity, and visual triage | The first scan adopts existing files without flooding Slack. A later Google Slides file is measured and, if deterministic checks pass, visually inspected in an owned thread; duplicate names and non-Slides uploads are refused. "Check the updated template" reloads the current source under the same native waiting state, while "run anyway" overrides only readable, non-structural listing warnings. |
| 2026-08-12 | A Slack upload keeps its aspect ratio until the measured frame is known | The old handoff cropped to the 4:5 canvas and placement cropped again to the template's often-wide frame, permanently deleting useful pixels. There is now one frame-aware crop, a pre-build crop-loss warning, local enlargement through 2x, and at most one fidelity-gated GPT Image 2 edit beyond it. Synthetic property generation remains unconnected and is not advertised as available. |
| 2026-08-12 | `/gable` commands queue mutating work onto the poller's owning thread | Bolt workers acknowledge immediately and may read through their own SQLite connection, but retries and same-run rechecks execute only after a successful current Sheet and roster refresh. A failed refresh defers the bounded queue on the normal schedule instead of using stale data or spinning. |
| 2026-08-12 | Only Carmen and Chase's stable Slack IDs may operate Gable | Channel checks alone let any member trigger paid calls and Drive changes. Production now requires exactly two IDs; stale OAuth client variables are rejected, and unused private-channel/interactivity capabilities were removed from the manifest. |
| 2026-08-12 | Human-owned contact sources are mirrored exactly and never web-written | The old lookup could plausibly overwrite client-facing identity, while deleting a workbook row left its old phone active locally. The roster is now replaced atomically from the current workbook; missing values pause and duplicate sources are refused. |
| 2026-08-12 | Corrected form rows preserve their deployed identity | Changing email or address changes the historical tuple hash and would otherwise reopen a finished listing. The unique form timestamp reconciles that change to the existing id, refreshes the saved payload, and makes a retry use current answers. |
| 2026-08-12 | Run transitions and paused-run claims are atomic | A current-state update without its `run_events` write made a listing unexplainable, and simultaneous human events could both build. Savepoints now bind row plus event, and only one worker can claim a paused run with its photo provenance. |
| 2026-08-12 | Two-agent requests fail closed until templates identify role-specific objects | Parsed listing and hosting roles do not prove which repeated text and portrait belongs to which person. The partial page-order path was removed; no copy is created until a certified slot contract exists. |
| 2026-08-12 | Template approval belongs to a **Drive revision**, and its Slack notice is a durable second step | The scanner compares `modifiedTime`, remeasures every changed revision, and listing clearance refuses an approval for older bytes. It persists the verdict as notification-pending before posting, so a Slack failure retries only that message rather than another paid visual call; deleting the source revokes a prior ready verdict. |
| 2026-08-12 | Replacement photos return to the source frame's **original z-order boundary** | Sending every hero to the back hid it behind imported raster art, while bringing every headshot to the front covered decorations. A new image starts frontmost; page-element order now identifies only the elements originally above the deleted frame and brings that set back to the front, leaving the replacement at the frame's former boundary while preserving relative order. |
| 2026-08-12 | Backfill, state transitions, and startup recovery are transactional | Catalogue adoption and its ready marker commit together; an old orphan marker is repaired rather than trusted. Runs interrupted by process death are marked failed on boot, row corrections reconcile by form timestamp, and duplicate active starts are refused before paid work begins. |
| 2026-08-12 | GPT-5.6 conversation tools use the **Responses API with explicit reasoning** | A live check found Chat Completions rejects Sol function tools at its default reasoning level. The Responses API is OpenAI's recommended reasoning/tool path; Gable now requests medium reasoning, parses direct function calls, fails plainly on incomplete/refused output, reserves ten conservative cents instead of the obsolete mini-model penny, and was verified live to ask whether “update the image” means the hero or headshot. |
| 2026-08-13 | **Reverses the `/gable` operator-command surface:** Slack is natural-language only | Chase rejected status, run, retry, templates, pause, and resume as user work the product should not require. The manifest no longer declares a slash command or `commands` scope; Bolt no longer registers one; the command service and poller's operator queue and pause controls are deleted. Mentions and owned-thread replies remain, including a plain-language request to reload a corrected source and continue a paused listing. |
| 2026-08-13 | **Narrows the no-web-contact rule:** validate every name, email, and direct phone before Slack; official-site fallback may fill only a workbook blank or a source-required credential for the current run | Chase's ordered workflow makes contact readiness a prerequisite rather than something discovered only if the chosen template has that field. A complete exact workbook row causes no web request unless the source requires a title the workbook does not collect. For an absent or incomplete row, or a title field such as REALTOR, Gable searches the one official Corner House Realty domain for one exact-name profile and requires the submitted email plus one direct phone in that profile's contact block. It does not overwrite the workbook or roster, never infers a credential, never substitutes an office/footer phone, and any submitted/workbook/site conflict pauses instead of being "corrected." Field-level provenance is written into the run-event detail without storing contact values there. |
| 2026-08-13 | Owned-thread clarification receives bounded Slack history and persisted listing facts | A terse answer such as “the big one” is meaningful only after Gable's prior hero-or-headshot question. The listener supplies up to twelve earlier turns plus the run's status, address, template, agent, output and photo facts. A confirmed hero replacement retains the current flyer until the new upload passes the ordinary build gates; a headshot replacement waits on `Head Shots`. |
| 2026-08-13 | Readable text overflow is corrected automatically, not presented as template work for Carmen | Exact Slides geometry determines the largest fitting size. Gable applies that font size only to fields it filled, keeps names, phones, emails and similar values on one line, reports the reduction in the final outcome, and still stops at the 8-point readability limit or when the source structure cannot be measured safely. The rendered vision inspection remains the final clipping and overlap gate. |
| 2026-08-13 | **Reverses the pre-build crop approval:** a supplied photo is fitted before Gable asks about layout | Chase's live test reached eighteen messages because every correctable fit became user work. A large center crop now becomes a truthful note in the one outcome after the build; it never asks “run anyway.” Structural defects, missing facts and unreadable text still stop, and the rendered vision inspection remains the fail-closed gate for a crop that removed important property content. |
| 2026-08-13 | A documented pre-inference image rejection can receive one **append-only operator release** | Reservations remain in the spend total and ordinary failures still consume the listing allowance. Only a human naming the exact spend row and durable evidence may append a release after the provider rejected an invalid request before model execution; runtime never does this automatically. The released reservation is excluded only from the per-listing image-operation count, and a unique database constraint plus an immediate transaction keep the replacement at one actual image-model call. |
| 2026-08-13 | GPT Image 2 final-photo edits use **high quality and a constraint-valid proportional canvas** | The Mike test exposed a client bug: rounding its 1078×504 frame to 1088×512 produced only 557,056 pixels, below the documented 655,360 minimum, so the API rejected it before inference and local stretching looked visibly pixelated. The chooser now enforces both 16-pixel edges, 3:1, 3,840-pixel, and total-pixel bounds; that frame becomes 1184×560. A vision-rejected draft stays internal rather than giving Slack a link to known-bad work. |
| 2026-08-13 | A proved source-photo contradiction routes directly back to the same thread's upload state | The strict visual result carries a typed remedy and a source-evidence flag rather than asking runtime code to parse prose. Only a checked, confident `replace_photo` verdict whose contradiction is independently legible in the first human upload, with no separate build/readback problem, becomes `needs_photo`; a detail legible only after enhancement stays `needs_review`. The rejected Drive copy stays internal, the one replacement request resumes the same run, and no web photo is selected. |
| 2026-08-13 | **Reverses generative real-photo upscaling:** every property-photo fit is deterministic and source-only | The Mike test showed a model can invent a crisp factual detail that was unreadable in the upload. Beyond 2x, Gable now contains the complete source at no more than 2x over a blurred, darkened fill made from that upload, at the exact measured frame size. No image provider, paid reservation, retry, or `ai_enhanced` claim remains on the live path. |
| 2026-08-13 | Every run question and outcome is a durable, Slack-confirmed outbox item | Posting a root or reply and changing SQLite state cannot be one transaction. Gable stores exact wording and target state first, then a process-lifetime loop independent of Sheet polling retries questions, review notices, failures, interruption notices and final links with one stable Slack client identity. After acknowledgement loss, only one exact Gable-authored text in bounded root/thread history confirms delivery. A verified flyer remains `building` until its link is confirmed; Slack loss cannot terminal-fail it or hide review/failure detail. An owned-thread photo atomically satisfies its pending request before preparation. |
| 2026-08-13 | Duplicate form timestamps and physical row movement are reconciled from a complete source snapshot | The live production tab contains both distinct and apparent duplicate responses with the same timestamp, so rejecting the tab or treating time as identity stops polling. New rows use a tab-and-row provisional id, while whole-tab reconciliation preserves stored ids across inserts, deletes and reorderings. A durable source-row alias ledger carries legacy duplicate multiplicity forward without replay and still lets a later byte-identical response open one new submission. Ambiguous corrections fail closed rather than merge two listings. |
| 2026-08-13 | Thread ownership requires a Gable root or an allowed person's root mention | The earlier ownership rule did not distinguish a human operator from another app or an unauthorized channel member mentioning Gable. A bot-authored root never transfers ownership, and a human-authored mention root does so only for Carmen or Chase's configured stable ID; direct mentions in foreign threads remain one-turn interactions. |
| 2026-08-13 | An answering photo upload is claimed durably before it retires its question | Retiring the request before the slow refresh, download and publish is what stops a delivery worker reposting it mid-upload, but on its own it loses Carmen's only image if the process dies during preparation: Slack does not redeliver the file, and nothing is left owed. The handoff now writes a `file_share` claim in `slack_event_claims` before that work and completes it only once an outcome is durable. The process-lifetime notification worker reads incomplete claims every pass, and either releases one whose run already recorded the upload or asks for the image once more in the same thread. It never asks twice, and never speaks over a message the thread is already owed. |
| 2026-08-13 | The visual inspection's output budget must cover its own reasoning | Reasoning tokens are drawn from the same `max_output_tokens` as the answer. At `effort: high` the Louis Smith flyer spent 886 of the 1000-token ceiling thinking, leaving ~114 for the JSON verdict, which truncated mid-array. `parse` correctly refused to read it, so a correct build was reported as "the visual inspection could not run". The reply came back `status: completed` with `output_tokens` exactly at the cap, so nothing distinguished a cut-off verdict from a malformed one. The ceiling is now 4000 and an exhausted budget is named in the log. |
| 2026-08-13 | The agent portrait is fitted as PNG with its alpha intact; only the property photo is matted onto white | The Corner House designs place the agent as a transparent cut-out that blends into the page and deliberately overlaps the address panel. Fitting it through the JPEG property path composited that alpha onto white, so the cut-out became an opaque rectangle whose corner covered the address box — two independent visual inspections reported it on the first Louis Smith flyer, and rendering the Sold source beside the output showed the difference. `fit_bounded_portrait_locally` keeps the identical geometry and memory guards and emits PNG. Matting remains correct for the full-bleed hero photo, where invisible RGB would otherwise become visible property detail. |
| 2026-08-13 | A wide hero crop takes its loss from the bottom, not evenly from both ends | The hero frames are letterboxes — the Sold design is 1078x504, or 2.14:1 — while property photos arrive at 3:2 or 4:3, so filling the frame always discards 30-38% of the height and the only choice is which end loses it. Centring split the loss evenly and sliced both gable peaks off the Dawn Rea flyer while leaving a large empty lawn below. A house's roof is the top of the subject and the driveway is the expendable part, so 20% of the loss now comes from the top and the rest from the bottom. Not zero: pinning a roof against the frame edge reads as cropped even when nothing was cut. Horizontal trimming stays centred, because left-to-right framing is the photographer's. |
| 2026-08-13 | Agent name headers match by prefix, so the live form's wording is read | `Form Responses 1` asks "First Name of Agent" and "Last Name of Agent"; `Testing_1` still carries the older "First Name" / "Second Name". The exact-match rule therefore found no name column on production, `maps_a_response_row` refused the entire tab, and polling could not have read one genuine submission — while every live test passed, because every live test ran against `Testing_1`. Found the first time `tools.preview_poll` was pointed at the real form. The three required fields keep exact matching, and `address` stays exact so the long "please provide the property address" question cannot outrank the real `Property Address` column. |
| 2026-08-13 | A complete contact-workbook row is cross-checked against the official profile; strict on the direct phone, silent on the name | A complete row previously ended validation with no web request, which trusted "filled in" to mean "correct". The row for `samuel@cornerhouserealty.com` carried Sam Johnson's name and email beside `443.509.4299` — Samuel Smith's direct line, a different agent at the same brokerage. Every existing guard passed: the row was internally consistent, the submitted name agreed with it, and the deterministic audit compares against the roster, which was itself the thing that was wrong. It stopped only by accident, because the headshot file was named for a surname the roster did not carry; had that file matched, the flyer would have shipped a stranger's phone number to a client. Only the phone is compared, because the email is proven by two prior exact checks and `lookup_official_profile` returns a profile only when the submitted address appears on it. The name is deliberately excluded: the official site says "Bobby Carr" while he brands himself "Bobby Carr The Dog Walking Realtor", neither is wrong, and a check that pauses on branding teaches its reader to ignore it. An unreachable or reshaped site yields to the workbook rather than halting every listing — the workbook remains the designated authority and the website is corroboration, so a check that cannot run must not become an outage. |
| 2026-08-13 | Every pause announces the listing in the channel and asks inside that thread | Only the photo request carried an announcement. Every other pause — a contact problem, a research gap, a missing design — posted its bare question as the channel root, so Chase saw "I could not find the square feet, list price for this one. Do you have them?" sitting in the channel naming no listing, agent or address. A question with no visible subject cannot be answered by anyone who was not already watching the run. `_ask` now opens with `people.opening_for`, which returns nothing when the run already owns a thread: the question store refuses a headline that would replace an existing root, and a resumed run's thread is already about this listing. |
| 2026-08-13 | An official profile is located by name but proven by a contact detail | Requiring an exact name match denied Bobby Carr every design that prints a credential: the site calls him "Bobby Carr", he brands himself "Bobby Carr The Dog Walking Realtor", and WordPress search returns nothing at all for the branded form because it requires every term to match. The name now only nominates a candidate — exactly, or as a whole-word prefix with a branding suffix, retried with the first two words when the full name finds nothing. Identity is then proven by the submitted email appearing on the profile **or** the contact workbook's filed direct phone matching the profile's single direct phone. Bobby submits from a personal gmail the brokerage page does not list, so the phone is the only proof available, and it matches exactly. A profile matching on neither is somebody else whatever the page is called; verified live that a wrong or absent phone still refuses. |
| 2026-08-13 | A request type already beginning with "New" keeps its own wording | `announce` prefixed blindly and produced "New New Listing request from Lolo Simmons", which is the first thing Carmen reads about a listing. Three of the six live request types begin with "New". |
| 2026-08-13 | A headshot well's overlap guard asks a different question of a neighbour than of something sitting on it | The guard measured overlap against whichever element was smaller, so any small element touching a frame's bounding box rejected it. That cost New Listing and Under Contract their headshot wells entirely, and both would have delivered carrying the template's sample face — Kelsey's and Kelli's — which is the exact defect the guard was written to prevent. A cut-out portrait imports with a transparent margin, so the "Under Contract" title band and the "4 Bedrooms" stat line graze its box by under 8% of the frame while lying more than 90% outside it. The rule now splits: an element more than half inside the frame is *sitting on* it and keeps the tight 4% tolerance, which preserves the decorative speech-tail case that motivated the original measure; an element that merely clips the edge is a neighbour and is judged on how much of the frame it covers, against the ordinary 25%. Five of six designs now resolve a headshot. Client Review Post correctly resolves none: its large image is the reviewing client's photo, and the agent appears only as text in the footer bar. |
| 2026-08-13 | A stated fact is its own provenance, outranks research, and resumes the run | Answering Gable's question did nothing. Chase replied "List price is $200,000", Gable said "Thanks, I've got the list price as $200,000", the run stayed at `needs_info`, and the value was never stored — `property_facts` for that address still had an empty price. The brain had no tool for it, so a plain answer fell through to a conversational acknowledgement. That makes every question a dead end, which is worse than not asking. `supply_listing_value` now records the answer and continues the same paused run. The value lives in a new `supplied_facts` table rather than in `property_facts`, because the provenance differs in kind: `property_facts` holds one row per address with one `source_url`, so a later scrape would silently overwrite what a human stated and a human value would erase the scrape's audit URL. Kept apart, `research_gate.resolve` seeds `known` from stated facts before planning — which also skips the paid lookup entirely when the answer covers every gap — and a lookup fired by some other missing field merges underneath rather than over them. Restricted to the five fields a design displays and research can miss; a Sold closing price and a Price Reduction's new price still come only from their own form columns. |
| 2026-08-13 | A listing value nobody has is Carmen's decision, not a dead end | Chase's rule: whatever is on the sheet is what there is, so if there is no price there is no price. Gable asks Carmen for the fields it could not answer; she either supplies them or says to build anyway, and then it builds with those fields blank for her to fill in. Before this, a gap research could not close stopped the run permanently — `research_gate` returned ASK and `preflight` raised a blocking `missing_value_*`, and neither had any release. `build_with_blank_fields` records the approval on the run itself (`runs.approved_warning_codes`, previously vestigial) so it covers that listing only and the next one asks its own question. The release is deliberately narrow: `preflight.blocking_after_release` frees `missing_value_*` and nothing else, so an unreadable type size, an ambiguous headshot well or an unsafe structure still stops the build, and the runtime refuses it outright while a photo is still owed. A blank is a blank — no value is invented to fill the space. |
| 2026-08-13 | Same-named official pages are resolved by contact detail, not refused on the count | Melanie Humeniuk's profile and her open-houses page are both titled exactly "Melanie Humeniuk", so the exact-name search returned two candidates and `lookup_official_profile` refused — denying her every design that prints a credential. Counting was never the right question: the name nominates, and the contact detail decides. Each candidate is now read (capped at three) and kept only when it carries the submitted email or the filed direct phone; a credentialled page is preferred over its untitled twin so an empty title is not reported as a missing credential. Pages that disagree on the direct line are still refused outright — that is not one person, and choosing between them is the guess this module exists to avoid. |
| 2026-08-14 | Everything outstanding is asked for in one message, once | Chase's rule: one list, one round of answers, then the link — the human does not want to sit answering a series of questions. Gable paused once per missing thing across up to twelve separate checks, and `_ask` posted only the first question while discarding the rest, so a listing missing three values and a photo took four round trips before any work started. The runner now gathers into a `needs.Needs` as it walks its checks and asks for the photograph and every unsettled value together. Two kinds of stop stay out of the batch, because neither is a value a reply can supply: a contradiction such as an address reading as a review link, which cannot be left as a placeholder and must not be guessed past, and a structural stop such as no design file named for the request type. |
| 2026-08-14 | A value nobody answered leaves the design's placeholder and the flyer still ships | Extends the 2026-08-13 blank-fields decision from a release a person requests to the default outcome of the one batched ask. Because the ask itself promises that anything left out keeps its placeholder, silence is a usable answer and Carmen never has to reply in order to decline; the ask records that promise on the run, so a resume never asks the same question twice. Delivery required three gates to learn the difference between an expected placeholder and a defect: `manifest.validate` now distinguishes an absent value from a malformed one and only forgives the absent; `judge` is given the exact literals of unfilled fields and removes only those before scanning, so the template's own sample agent is still caught; and the visual verdict is filtered afterwards by problem kind, forgiving only `placeholder` findings and only when the categories line up one-to-one with the problems, so a clipped line or a bad crop still stops delivery. The finished-flyer message names every field left showing — the prohibition that survives is the silence, not the gap. |
| 2026-08-14 | Releasing a run to build with blanks must not move it out of its pause | `approve_blank_fields` set the status to `pending`, and the very next call in the same path — `claim_paused_run` — requires the run to be in `PAUSED`. So the claim always failed and every release answered "This listing is already being rechecked or is no longer waiting", which read as a race and was in fact a state machine talking to itself. Nothing built through that path since it was written, and no test covered it. The release now records the code and its event without touching the status. |
| 2026-08-14 | The spend ceiling is configurable, and defaults to the $50 Chase set | A deliberate campaign across every design and every salesperson reserves far more than day-to-day use — roughly ten cents per visual check, per conversational reply, and a cent per search — so the compiled-in ceiling would have stopped the campaign partway and reported it as a vision failure rather than as a budget. `GABLE_SPEND_CEILING_USD` is read once into the frozen settings and applied at startup, bounded so a mistyped variable cannot authorise unbounded spend. The default is unchanged, and an entry point that forgets to apply it fails closed at $50 rather than open. |
| 2026-08-14 | One reply answers the one question, so the tool takes a list | `supply_listing_value` accepted a single field, and only the first tool call in a response was ever read, so "3 beds, 2 baths, $600,000" recorded one value and discarded the other two — then the run paused again asking for what had just been supplied. Batching the question (above) guarantees the multi-answer reply, so the tool now takes an array and every entry is stored before the run continues. A malformed entry is skipped rather than discarding the values sent beside it, and the older single-field shape is still parsed so a decision persisted before this change is not read as an empty answer. |
| 2026-08-14 | The message carrying the photo is read for answers before the run resumes | Gable asks for the photograph and the missing values in one message, so the natural reply is one message carrying both — and Slack's file-share route returned before the text was ever looked at, so the caption was silently discarded and the flyer built with placeholders for values the person had just typed. The caption is now read and stored before the resume that builds with it. Two guards keep this cheap and safe: every value the ask can take is a number or a date, so a caption with no digit never reaches a paid call, and a caption that cannot be read is logged and passed over rather than costing Carmen the photograph she just sent. |
| 2026-08-14 | A delivered flyer can be built again from its own thread | "Run it again" is the most natural thing to ask after reading a flyer, and it was the one thing that could not work: `delivered` is terminal, so `claim_paused_run` refused it and the reply was "this listing is already being rechecked or is no longer waiting". `reopen_for_rebuild` returns the run to a human-owned pause inside the same transaction as its Slack event claim, so a redelivered request cannot rebuild the same flyer twice, and no second attempt is opened against the three-attempt ceiling. The phrase set also did not contain Chase's own wording — "run it again" — so the request read as ordinary conversation; the recognised phrasings are now one named constant. A new image plus those words replaces the photograph and rebuilds through the existing replacement action; an image dropped into a finished thread saying anything else still leaves the flyer alone, because an accidental upload must not replace something already approved. |
| 2026-08-14 | Test rows are appended by one write-scoped tool, fenced to the Testing tabs | A campaign across every design and salesperson needs rows to exist, and there is deliberately no synthetic-submission path: the point of a live test is that it walks the same header discovery, identity assignment and reconciliation a real submission does. The runtime stays read-only; `tools/seed_test_rows.py` is the only writer, and it refuses any tab whose name does not begin with "Testing" before it builds a Google client at all, so the form's own response tab cannot be reached however it is typed. It appends and never updates. It writes by header text rather than position, which matters because `Testing_1` and `Form Responses 1` do not share a column order — the same layout mismatch that hid a production header bug until 2026-08-13. It also refuses a request type with no design and an agent absent from the contact workbook, since both open a run that stops before it can test anything. Verified live that the service account already holds edit rights on the workbook, so no permission change was needed. |
| 2026-08-14 | A photo is drawn at the design's inner guide, not at the edges of the shape it replaces | The first campaign run delivered an Under Contract flyer with the word "Realty" sliced in half, and the visual gate caught it. The well carrying the sample photograph runs 95,212 EMU up behind the Corner House logo; the source gets away with it because Slides letterboxes a picture fill whose aspect does not match its shape, so the sample never reaches the top of its own box, while Gable crops to fill and does. The first attempted fix — pointing the design at the smaller shape — was wrong and was caught by the very next run, which rendered the template's own two-storey sample house: the sample lives in the well, so the well is what must be deleted. The imports mark the real picture area with a second empty rectangle inside the well, so the two roles are now separate. The well is still replaced; the guide is where the image is drawn. Required containment of 95%, at least half the well's area, and a top edge meaningfully lower than the well's, because pulling the photo down out from behind the design's furniture is the entire purpose. Measured against all six live designs: only Under Contract carries such a guide, so the other five are untouched. The geometric search deliberately keeps refusing rather than applying a guide, since an unknown template offers no evidence that the smaller shape means anything.
| 2026-08-14 | A design whose sample photo is split across two shapes must have both removed | New Listing delivered a flyer with two houses on it: the template's own brick mansion above Chase's cottage. Its photograph is two overlapping shapes — `p1_i90` across the top of the slide behind the logo, `p1_i92` the band below — and replacing only the recorded well left the other layer showing. Established by copying the design and deleting each shape in turn rather than by reasoning about the geometry: deleting `p1_i90` alone leaves a clean logo band with the full sample beneath it, deleting `p1_i92` alone leaves the sample above and a white gap below. Recorded per design in `slides/designs.py`, evidence-only, and deliberately empty for the other five — Sold's second shape is the white panel behind the Corner House logo, and deleting it once left the logo washed out over the brickwork. |
| 2026-08-14 | The design draws its own credential mark; Gable supplies only the word | New Listing rendered "Realtor® ®" because it draws a superscript ® beside its own REALTOR placeholder while Gable filled the value complete with the symbol. Under Contract has no mark at all in its source, and Gable was adding one there. Filling the title as the plain word lets each design keep the styling its designer chose, which is the same rule the rest of the fill obeys. It also shrinks less: the Sold title now autofits to 16.65pt rather than 14.93pt. |
| 2026-08-14 | Square footage is filled as the number alone | Every design puts the figure beside a ft² icon and its own "Sq FT" label, so a value carrying the unit renders as "1450 sq ft" next to a square-foot symbol. A person answering Gable's one question types the unit naturally, and correcting them is worse than normalising it. A value with no digits at all is passed through unchanged rather than blanked. |
| 2026-08-14 | A design-typeset credential keeps the placeholder's capitalisation | New Listing sets REALTOR in capitals with a separately positioned superscript ® immediately after it. Filling the title as "Realtor" left that mark stranded in open space, because the lower-case word is narrower than the placeholder it replaced — the flyer read "Realtor    ®". The rule is deliberately narrow: only `agent_title` follows the placeholder's case, because it is a fixed word the design typesets rather than something a person wrote. An address under a "PROPERTY ADDRESS" placeholder and a name under "AGENT NAME" are returned untouched; upper-casing those would rewrite what somebody typed. |
| 2026-08-14 | The Open House design's live sample beds, baths, date and time are recognised | Open House stopped at needs_template complaining about a fillable-looking "5 BEDS" it could not name. Its beds, baths, date and time are written as a previous listing's real details rather than bracketed tokens — the third convention this deck uses, which square footage already handled and beds and baths did not. Leaving them unrecognised was not neutral: an unfilled box keeps Louis Smith's actual open-house date and time, so somebody else's open house would print on the flyer. The date and time sit in separate boxes, so a supplied "Saturday, Sep 6, 2026 1-3PM" is split between them; details carrying no recognisable time fill both boxes with the whole string rather than leaving either showing the sample. |
| 2026-08-14 | The listing announcement belongs to the first ask only | Batching every outstanding need into one message made that ask reachable on a resume, and it always carried the announcement. The question store refuses a headline that would replace an existing thread root, so the whole notification was rejected and the run died telling Carmen a processing step had failed. Caught live when a date clarification resumed a run that still owed its photograph. The ask now takes its opening from `people.opening_for`, which already returns nothing once the run owns a thread, and which now also accepts the proven contact name so the first announcement still uses it. |
| 2026-08-14 | Open House replaces the shape carrying its sample house, not the sky behind it | The flyer rendered the template's own stone-and-siding colonial instead of the supplied cottage, and the visual gate reported a completely different property. `p1_i104` — the recorded well — holds a sky backdrop, and the sample house is attached to `p1_i105` in front of it, so the supplied photo was drawn underneath and never seen. Established by copying the design and deleting each shape in turn, the same way New Listing was settled. `p1_i105` runs up behind the logo and `p1_i104` sits inside it starting lower, so the placement guide already added for Under Contract draws the photo in exactly the band the design shows. Three of the six designs have now needed their well corrected against a render; a geometric rule alone has been wrong on every one of them. |
| 2026-08-14 | A field inside grouped artwork is measured through its group, not refused | New Listing with Open House was rejected on every run: its REALTOR box lives inside a group scaled to 0.75, and `text_boxes` read each element's own transform, which is relative to its group. The measured width was a third too generous, so preflight refused the design outright rather than trusting it — an honest refusal of a real measurement gap. The gap is now closed instead: `text_boxes` composes parent transforms through `absolute_boxes`, and scales the declared point size by the same factor, because a group scales the type along with the box. Rotation and shear still stop the build; those change what "fits" means rather than merely scaling it, and the rendered vision pass remains the final gate either way. The two scales are kept apart: an element's own transform shapes its box — that title is a 3,000,000 EMU square scaled to 1.11 x 0.13 — and does not resize the type inside it, while a group's scale does. Multiplying both into the font read the title as 1.79pt and refused the design a second time, as unreadable. |
| 2026-08-14 | New Listing with Open House needs its sample-house layer removed, and reads its open house from one two-line box | Two defects on the same design. Its photograph is two shapes like New Listing's — `p1_i92` is the band the design shows, `p1_i93` carries the sample house on top — so replacing only `p1_i92` put the supplied photo behind the template's own brick colonial. And it writes the open house as a single box holding two lines, "SUNDAY, MAY 24TH\n1 PM - 3 PM", which neither the date nor the time pattern matched, so the tag kept a previous listing's real open house. That box is now recognised and filled on two lines, because putting the whole string on one line overflows the tag it sits in. |
| 2026-08-14 | A shape the same batch deletes is never named in the layer reorder | New Listing with Open House deletes a sample-photo layer that sits ABOVE its well, and the z-order request still listed it. Slides refused the whole update, so the flyer built with no photograph on it and Gable correctly reported that it could not finish. New Listing did not show this because its extra layer sits below the well and never entered the reorder. |
| 2026-08-14 | The read-back verifies the text that was written, not the value before it was adapted | New Listing with Open House was refused for "the agent title did not make it onto the flyer", on a flyer that plainly showed REALTOR. The title is filled in the placeholder's capitals to keep the design's own credential mark tight, so the written text was "REALTOR" while the check still looked for "Realtor". Verification now reads the substitutions that were actually sent, which is the only thing a read-back can honestly confirm. |
| 2026-08-14 | Client Review Post's heading, sample testimonial and sample agent are all read correctly | Three separate gaps on one design, found by running it. "CLIENT TESTIMONIAL" is the heading above the quote, not a field, and Gable stopped at needs_template calling it a fillable field it could not identify — it joins the brand copy that is never replaced, along with "REAL PEOPLE / REAL RESULTS" and "Let's Find Your Corner." (with the curly apostrophe the design actually uses). The sample review was anchored on "Working with Corner House Realty…" but the live design opens "Review goes here...Working with…", so the anchor missed it. And "Sebastion Johnson" in the footer was in no sample list, so the agent name never resolved and that name would have printed on another agent's testimonial flyer — the exact defect the sample lists exist to prevent. |
| 2026-08-14 | A reviewer named inline — "Sarah Whitfield said: ..." — is read rather than asked about | The parser required the reviewer's name on a line of its own, which is how the real submissions are written, and asked whenever it could not find one. That is right for vague prose, but "Sarah Whitfield said:" states whose words follow, so reading it is not a guess. Only "said", "wrote" and "says" count, and only at the very start; a name inside the body still resolves nothing, because "Gina was outstanding" names the agent being praised rather than the client praising her. |
| 2026-08-14 | Text width is measured from the designs' own faces, not estimated from character classes | Annie Nowicki's Open House flyer wrapped her name onto two lines and dropped the second on top of the Realtor title beneath it. The fitter had shrunk the name to 18.78pt and stopped there, because the five-class width table put it at 116.15pt inside a 129.07pt box — a 14 percent underestimate, which no 10 percent safety margin can absorb. The table averages every lowercase letter to one width, so it cannot separate a name full of `w` and `k` from one full of `i` and `t`; widening the classes or the margin would shrink every other flyer's type instead. `slides/typemetrics.py` now carries the real advance width of every character in the six faces these designs draw text in, measured by writing each character 12 and 24 times into a real text box, rendering both, and subtracting — the difference cancels side bearings exactly. Verified three ways against the same 129.07pt name box: the design's own "Louis Smith" at 22.12pt sums to 128.99pt and renders on one line; "Annie Nowicki" at 18.78pt sums to 135.16pt and wraps; at 17.50pt it sums to 125.95pt and does not. These designs carry no text inset, so the usable width is the box width. A measured face gets a 2 percent margin rather than 10, because a wide margin on an exact number is just smaller type than Carmen drew. Any other face still falls back to the class table and its original margin. |
| 2026-08-14 | The font a box is set in is read from its first visible run | Every filled box in these designs ends with a newline run left at the imported default, Arial 400, whatever the visible words are set in. Reading the first run rather than the first visible one would have called bold Open Sans regular Arial and measured both the weight and the width wrongly. |
| 2026-08-14 | Every field is filled through a sentinel, so nothing Gable writes can be replaced again | Bobby Carr's Under Contract flyer read "Bobby Carr The Dog Walking REALTOR". His brokerage name ends in "Realtor", Under Contract's title placeholder *is* "Realtor", and the title replacement ran after the name was already on the slide. The standalone-literal proof passed honestly: every literal was standalone in the design, and the collision only existed once Gable's own value was there. New Listing escaped it only because its placeholder is already capitalised, which is luck rather than safety. Each field is now filled in two passes — literal to a fixed-width private-use sentinel, then sentinel to the value — sent as one atomic batch, so no pass ever searches for a word and no ordering can matter. The read-back caught the corruption and stopped delivery, which is the gate working; it should not have had anything to catch. |
| 2026-08-14 | Photo placement moved out of `pipeline/live.py` into `pipeline/placement.py` | `live.py` crossed the 800-line ceiling. The seam that was already there: `live.py` is the module that holds credentials and wires the sequence, while template clearance, hero and headshot placement, and the z-order restore are about putting an image onto a design without disturbing it. 360 lines moved unchanged. |
| 2026-08-14 | The fill sentinel is plain ASCII, because Slides strips private-use codepoints | The two-pass fill first used U+E000 to wrap each sentinel, on the reasoning that a private-use character cannot arrive from a design or a form. Slides removed it: `replaceAllText` reported success, the document stored `G0000` with the marks gone, and the second pass matched nothing. Two flyers were built carrying bare sentinels — neither was delivered, because the occurrence check reported that not every field changed and the runs stopped at needs_review. The marker is now `{{GABLE-NNNN}}`, and a design or a value already containing `{{GABLE-` refuses the fill rather than risking ambiguity. |
| 2026-08-14 | `run_row.py` can start a run from an already-published photo, for breadth testing only | Exercising one design across thirty-eight agents means the same photograph goes into thirty-eight threads, and driving that through the Slack web client proved unreliable enough to stall a night's testing: uploads sometimes did not attach, sends sometimes did not fire, and both reported success either way. `--hero-photo-url` starts a run with the photo already in hand so the renderer can be tested at breadth. It is explicitly not how a listing gets its photograph — that is the one thing a person chooses, and the thread ask is unchanged. Worth knowing it exists: anyone with droplet access can now build a flyer without Carmen picking the image, which is a capability the product did not previously have. |
| 2026-08-14 | The visual gate is told which sample text was deliberately left showing | A New Listing flyer with no supplied price keeps the design's own "$350,000", and the inspector reported it as using a period instead of a comma — a correct flyer parked in review over a comma the model misread. The gate was being told nothing and its verdict filtered afterwards, which only works when the model classifies the finding as a placeholder; this one came back as text. The prompt now names the exact strings that were left on purpose and says they are expected and correct, while still asking for them to be reported if they are clipped, overlapping, or too small to read — the only problems Gable could have caused there. The post-filter stays as a second net. |
| 2026-08-14 | A failed run records the kind of error, never its message | Three runs out of about 165 failed overnight at the same point — just after preflight, during the build — and every one recorded the same sentence, so none could be told apart without reproducing it. All three delivered on a rerun, which points at a transient Google call rather than anything in the listing. The recorded reason now names the exception's class. Not its message: a Google error body can carry a signed URL and a Slack error can carry an id, and this string is written to the database and said in Slack. |
| 2026-08-14 | A filled measurement keeps the design's own unit and capitals | Answering Gable's question with "4 beds, 3 baths, 2,450 square feet" rendered "4 beds", "3 baths" and a bare "2,450" on a design that writes "5 BEDS", "5 BATHS" and "6,348 SQFT" — the unit vanished and the capitals with it. New Listing was worse: it sets its counts on two lines as "4\nBedrooms", and a supplied value replaced the whole box with "4 beds". No listing had ever had these filled correctly; every flyer that showed a unit was showing the design's untouched sample. The number is now composed with whatever trailing text the design's own placeholder carries, so the box reads the way the designer drew it whoever supplied the figure. A placeholder that writes no unit — a bracketed one, which labels itself elsewhere — gets the number alone, and a value with no number at all, such as "Studio", is left exactly as it was said. The single-line fitting set is keyed on the written text for the same reason the read-back check already was. |
| 2026-08-14 | A value supplied after delivery reopens the finished flyer instead of being refused | "Can you run this again? The price should be $560,000" is the most natural thing to say after reading a flyer, and Gable answered "this listing is already being rechecked or is no longer waiting". The conversational model read it correctly as a supplied value; the value was recorded; and then the resume refused, because a delivered run is terminal and its claim rejects it. The design-updated path already had `reopen_for_rebuild` for exactly this reason, so the guard is now shared by both — a finished run is reopened inside its own event claim, so a redelivered Slack message cannot rebuild the same flyer twice, and a run that is merely paused continues as it always did. Found by testing the last part of the flow that had never been exercised live. |
| 2026-08-14 | Every listing announces itself before the build, not at its first pause | A run that never had to ask anything reached delivery owning no thread, so "Your flyer is ready" posted at channel level naming no property — Chase saw a column of bare links with no listing attached to them. The announcement used to belong to the first pause, which every real run happened to have because it always needed a photograph. It now happens once every prerequisite has resolved and before the build, so the thread exists whether the run ends in a question or a link, and the ask, the outcome and every later reply share one root. Placed after the contact, title and headshot checks rather than before them, because Gable must not announce a listing publicly and then immediately say it cannot build it. A Slack failure while announcing is swallowed: an outage must not turn a verified flyer into a failed run. |
| 2026-08-14 | A headshot frame is pulled back off whatever the design draws beside it | These designs place the agent as a transparent cut-out, so the frame's rectangle runs into things the person never covers: New Listing's reaches into the footer band and across the start of its slogan, and New Listing with Open House's reaches over the start of REALTOR. A photograph is opaque to its edges, so it painted over both and the visual gate reported each one. `slides/framing.py` now pulls the frame back on whichever single side costs least for each neighbour it only partly overlaps, refusing any clip that would take more than 30 percent of the frame — past that the design is not "a cut-out with room around it" and guessing at a much smaller portrait is worse than the overlap. A background the frame sits inside is not a neighbour to avoid. Kept as plain rectangle arithmetic so it is testable without a slide. |
| 2026-08-14 | A submission's own note about the deal fills Under Contract's call-to-action panel | Row 79's details column reads "Under contract on the buyer side. Multiple offer situation." and nothing carried it onto a flyer: post details were read only for a Client Review's quote, so on every other design the sentence an agent typed was dropped. Under Contract has exactly one free text block — the panel that ships reading "Ready to Buy? / DM me to find your next home." — and it sits inside the Under Contract band Chase asked for. Proven on a scratch copy of the live design: six of six replacements confirmed, and the sentence sets on four lines inside the panel. A note is only a note when it reads like one, so a blank column, a dismissal ("Na", "n/a", "None") and a full marketing paragraph all leave the design's own words alone. No other design carries a matching literal, so nothing else changes. |
| 2026-08-14 | **REVERSES a stop: a job title no design has room for falls back to the credential inside it** | Two roster records could not produce a flyer on any design. Sara Wolz's "Listing Manager, Transaction Coordinator & Realtor" needs about 627 percent more width than Under Contract's title slot and Gina Moore's "REALTOR, The Kulnich Home Team" about 391 percent, and both stopped as `needs_template` asking Chase to redraw the artwork — which is not something Carmen can do from a thread. Both titles already contain the credential the slot was drawn and measured for, so the flyer now prints that word and the closing message says the longer title was dropped. Three guards keep it honest: the shorter form is taken from the agent's own proven title rather than invented, so a title claiming no credential is still a stop; the full title is measured first and left alone whenever it fits; and the substitution is measured as it will actually be written, capitals included, because `agent_title` follows the placeholder's case. `preflight.Report` carries the adjusted value back so the fill writes exactly what was measured. |
| 2026-08-14 | The replacement-photo ask moved into `pipeline/questions.py` | `runner.py` reached the 800-line ceiling. Every label, reason and transition detail that pause needs is fixed, and `questions.py` is the module that owns persisting, posting and confirming a run notice — so `request_replacement_photo` belongs there rather than being spelled out at the one call site. The two unplaced-image outcomes were also folded into one: a missing hero photo and a missing headshot are the same outcome, a draft kept for retry and never called finished, and they were two near-identical blocks. |
| 2026-08-14 | An address a person states is stored over the submission, and it moves the run | Row 81 arrived as "1011 Winged Foot Drive" with no city, state or ZIP. Gable asked "What is the property address?", Chase answered it exactly, Gable replied "Thanks, Chase. I've got 1011 Winged Foot Dr, Bowie, MD 20721" — and nothing moved. `supply_listing_value` could carry a price, square footage, beds, baths and an open house, and no address at all, so the model correctly chose no tool and just talked. Gable was asking a question it could not accept the answer to, which is the same dead end a stated price was in before it. The address cannot live in `supplied_facts`, which is keyed by `address_key` — the whole problem is that there is no usable address to key on — and it must never be written back to the form. So `stated_addresses` belongs to the submission and `load_submission` lays it over the form's own value, which means the sheet re-read that every resume performs cannot quietly undo the correction. It is validated with the same rule that rejected the original, so an answer that is still not an address is refused rather than reproducing the pause with a value nobody can trace. A reply carrying both an address and a price records the address first, so the price is filed against the corrected property. |
| 2026-08-14 | Recording an answer and resuming its run moved into `slackapp/supplied.py` | `runtime.py` reached the 800-line ceiling adding the reload above. Recording a reply and continuing the run is one step rather than two — the whole point of the two defects it now carries in its docstring is that recording without resuming is a dead end — so it is one function, out of the tool dispatch and testable without building every production client. |
| 2026-08-14 | A date drops the word that joined it to a time now in another box | Row 81's open house reads "08/01 and 08/02 from 12-2pm". Open House sets the date and the time in separate boxes, so the time was taken out correctly and the date box was left reading "08/01 and 08/02 from" with the joining word dangling at the end of the line. Only a trailing joiner is removed — from, at, on, between, starting, beginning, @ — and only when it ends the remainder, so "Saturday, Sep 6, 2026" is untouched. |
| 2026-08-14 | Square footage is grouped the way every design writes its own sample | A researched 3663 rendered as "3663 SQFT" beside a design whose sample reads "6,348 SQFT", and New Listing's reads "2,430 Sq FT". Grouping a number is presentation rather than a change to what anyone stated, and the unit and capitals already come from the design's own placeholder. A value carrying no digits at all — "Studio" — is still passed through exactly as it was said. |
| 2026-08-14 | A design nobody has scanned yet is measured on demand, not refused | Chase edited the Open House design, asked Gable in the listing thread to check the updated template and rebuild, and was told "I could not match this listing to a reviewed source template". The catalogue is baselined by a scheduled scan that has never run on the live database, so `template_audits` was empty and `recheck_file` refused on a missing row. A design Gable itself selected and built that very flyer from is not an unknown file. It is now measured on the spot and its audit recorded, which is exactly what the first scan would have done. A file that genuinely is not in Generic Templates is still refused, because there the sentence tells Carmen where to put it back. |
| 2026-08-14 | A new design's character allowance does not govern rebuilding a listing already built on it | The Open House rebuild was refused because the design's agent-name box "cannot hold the safe test of 28 average characters" — on a flyer for Tambria Eaton, 13 characters, built from that same design fifteen minutes earlier. `TEMPLATE_CAPACITY_CHARS` is the standard for adopting a design nobody has built on yet: could this box hold a long-but-normal value from anyone on the roster. It is the wrong question for a listing whose own values the runner measures again before anything is copied. A listing-triggered recheck now drops `capacity_*` issues from both the verdict and the recorded audit — recording one would have been worse than refusing once, because `template_clearance` reads that audit and every future Open House run would then have stopped. Adopting or updating a design through its own source thread still applies the full allowance. |
| 2026-08-14 | Two open-house days at the same hours write that time once | Row 98 wrote "08/08/2026 11am-1pm , 08/09/2026 11am-1pm". Only the first time range was taken out into the design's own time box, so the flyer read "08/08/2026 , 08/09/2026 11am-1pm" with a separate "11am-1pm" underneath it. When every range in the value is the same time, all of them are removed and the design writes it once. When they differ they are two facts — dropping one would be a lie about when the house is open — so that value is left exactly as it was and a person can see both. |
| 2026-08-14 | A client review is never asked for a property address | Row 5's address column reads "Google Review, SRES Listing 29 Maple" — where the review came from, not a house — and Gable stopped to ask Chase what the property address was. A Client Review Post has no property: its design carries no address field, `fields.is_usable_for` has always known that through `ADDRESSLESS_CATEGORIES`, and nothing about it is researched. `incoherences` did not know it, so it demanded an address for every request type alike. The constant moved to `listings/intake.py`, whose vocabulary it is, and `slides/fields.py` imports it from there. A listing whose address really is unreadable still stops and asks. |
| 2026-08-14 | An agent's cut-out is contained in its slot, never cropped to fill it | Chase, on Kirby-Jay John's New Listing with Open House flyer: "The top of his head is cut off in the image and that is not acceptable." New Listing with Open House draws a square 233x233pt portrait well and every filed cut-out is 600x900, so the shared cover-crop took 150 pixels off the top and 150 off the bottom of the source — the crown of his head and his chest. A property photo is cover-cropped on purpose: it has to fill its band edge to edge and the lawn is expendable. A person is not. The portrait is now scaled until it fits inside the frame and placed on a fully transparent canvas of exactly the frame's size, centred horizontally and anchored to the bottom, because these designs stand the agent on the card's baseline. The spare room is transparent, so nothing reads as letterboxed. Under Contract's well is already 2:3 and is unaffected. |
| 2026-08-14 | A listing rebuild reloads its design but does not re-certify how the artwork looks | Asking Gable to rebuild Kirby-Jay John's flyer came back "the open house tag is cut off along the right edge" — which is New Listing with Open House's tag hanging off the page on purpose, exactly as it did on the flyer that had already delivered from that same design. Certifying a design's appearance is the question the design's own Slack thread asks, once, when the file is new or edited there. A listing rebuild needs the current bytes and a structural check; the flyer it produces is then inspected on its own render, which is the check that can actually see what Gable did. So the source visual pass is skipped for a listing recheck, which also stops a paid inspection running on every rebuild. The design thread's recheck is unchanged and still refuses on a visible defect. |
| 2026-08-14 | Client Review Post has its own manifest, with no address and no price | Having stopped `incoherences` from demanding an address for a review, the run hit the same wall one step later: no manifest was registered for the design, so it fell to `DEFAULT_LISTING`, which requires one, and row 5 was asked to separate the street, city, state and ZIP of "Google Review, SRES Listing 29 Maple". A testimonial has no property. Its manifest asks for the agent it belongs to, their contact, and the photograph the layout is built around — which the design does have, a tall well that a previously delivered review flyer filled with the house the client bought. |
| 2026-08-14 | A shorter pull-quote can be sent back, and it outranks the pasted review | Every client review on the live form runs 400 to 1,000 characters and the Client Review Post quote panel is drawn for about 280. Rob Morgan's 1,028-character review was fitted down past the readability floor and the rendered inspection correctly refused to deliver it — a correct stop with no way out, because `supply_listing_value` could not carry a quote and post-delivery edits are unavailable to a run still in review. `review_quote` joins the suppliable fields, so the shorter version a person actually chooses is what gets set. Only the quote: the reviewer's name is not the answerer's to change, and a quote with no readable reviewer under it is ignored rather than printed. Trimming a client's words stays a person's decision — Gable never shortens a review itself. |
| 2026-08-14 | An unreadable address rides the one batched ask instead of stopping first | Chase, on a nineteen-message thread: "If a user has to go back and forth 19 times they are just going to build it themselves. That is the problem." Asking for the address, waiting, then asking for the photograph is two waits for one person's attention when one would do. Batching it is safe because it cannot leak — `slides.manifest.ADDRESS_SHAPE` refuses to build a listing whose address is still unreadable, so an unanswered address stops the flyer either way; it just stops without having cost an extra turn. A price reduction with no new price is deliberately NOT batched: nothing downstream catches it, so silence would ship a price-reduction flyer with no price on it. |
| 2026-08-14 | A built flyer is measured against its design's geometry, not only looked at | Chase reported three defects the rendered vision pass had not: a grey band too large, a corner outside the page, and a footer bar running off the edge. All three are rectangles, and a vision model was never the right instrument for them. `slides/layout.py` compares the built flyer with the design it was copied from and reports only what changed — because these designs bleed on purpose (Open House's footer rule starts 408 points off the left edge) and a portrait is meant to overlap the band behind it, so an absolute check would cry wolf on all six. Bleed is compared per page edge, since a design that already runs off the bottom would otherwise mask a brand-new overhang on the right. Overlap is measured as intrusion depth rather than shared area, because two boxes drawn to touch along a 200-point edge share a lot of square measure and nothing a reader would notice. |
| 2026-08-14 | A reviewer's name can be answered too, so no question on a review is a dead end | The parser reads a reviewer's name only from a line that is nothing but a name, which is how row 5 was written. A Zillow export writes "7/20/2026 • j E", so rows 39, 49 and 50 all resolve no name, the design's `client_name` slot stays empty, and Gable stops to ask who said it — with no way to accept the answer. `client_name` joins `review_quote` in the suppliable fields. Between them, every question a Client Review can raise is now one a person can answer in the thread. |
| 2026-08-14 | The agent title is measured exactly rather than estimated, so it leaves the capacity table | Carmen edited the Under Contract design, the scheduled scan re-certified it, and it failed on a 24-character allowance for the agent title — which blocked every Under Contract run through `template_clearance`, on a slot that renders "Realtor" perfectly well. Every other entry in `TEMPLATE_CAPACITY_CHARS` stands in for a value nobody knows at certification time, so an average-width estimate is the best test available. The title is the one field whose value is known: a title too long for its slot is cut back to the credential before anything is written, and that exact word is measured against the box's real advance widths. Two tests for one field, one of them a rough estimate, is one test too many — the estimate goes. |
| 2026-08-14 | Text is fitted to the lines it really wraps into, not to a ribbon of total width | Donald Clark's flyer put the ZIP of "4812 Reisterstown Road, Baltimore, MD 21215" on a third line, on top of the panel beneath it, and the fitter had already declared it fitted. The width model asked whether the total advance width was under the box width times its line count — a ribbon that can be cut anywhere. Slides breaks at spaces, and "4812 Reisterstown Road," alone is wider than the 249-point box, so two lines' worth of text needed three lines. `wrapped_line_count` now walks the words greedily at the measured advance widths and counts the lines Slides will actually produce, including breaking inside a word too wide to sit on its own line. The ratio still gives the first answer, because width scales linearly with size; wrapping does not, so the result is stepped down five per cent at a time until the text really does break into the lines the box has. The rendered vision pass caught this one and refused to deliver, which is the gate working — it should not have had anything to catch. |
| 2026-08-14 | A character allowance advises; it no longer refuses a design or the listings on it | Carmen edited the templates and the scheduled scan refused Open House on its agent-name slot, Sold and Under Contract on their title slots — and because `template_clearance` reads that verdict, every listing on those designs stopped too. The allowance asks whether a box could hold a long-but-normal value from anyone on the roster. That is worth telling Carmen when she files a design, and it is the wrong instrument to gate a build with, because every real value is measured exactly against the same box moments before anything is copied. Capacity findings are now warnings: Gable says which slot is tight, adds that it will still fit each real value, and records the design as ready. Structural findings — slide count, a rotated box, an unmeasurable field, a duplicate name, a missing file — still refuse. The visual certification now also runs on a design whose only finding is advisory, which it previously skipped. |
| 2026-08-14 | A read that times out is retried like any other transient failure | Two manual runs and a whole poll pass died instantly on `TimeoutError` reading a 110-row sheet that normally answers in under a second. The retry loop only ever retried an `HttpError` carrying a retryable status; a socket-level timeout fell to the catch-all and broke the loop on its first attempt. Timeouts, connection resets, DNS failures and SSL errors are all transient transport conditions and now share the same bounded, jittered budget the retryable statuses have. An error retrying cannot fix — a bad range, a bad credential — still fails on the first try, because retrying it only delays the report. |
| 2026-08-14 | **REVERSES an earlier row: an open house with no time empties the time box rather than repeating the date** | The earlier rule filled both boxes with the whole supplied string when it carried no recognisable time, reasoning that a blank box would leave the design's own "2-4PM" — a previous listing's real time — on the flyer. That reasoning was wrong: an explicit empty replacement clears the box, so the sample is gone either way. Sydney Kinney's open house came through as "7/11/2026" and the flyer printed that date twice, once in each box, which reads as broken. The date box now takes the value and the time box is emptied. |
| 2026-08-14 | An address the design cannot print is asked for with the photograph, not after it | Kim Hixson's "8517 Oglethorpe Street, New Carrollton 20784" carries a ZIP and no state. It passes `address_looks_usable`, so the coherence check let it through, and the manifest refused it one step after the upload had already arrived — a whole second round trip for a question that could have ridden the first. The manifest's own address shape is now consulted before the batched ask, so a design that shows an address asks for a whole one in the same message as the photo. A design with no address slot is never asked, which is what keeps a client review quiet. |
| 2026-08-14 | A created image inherits the overhang of the frame it replaced | The new layout audit refused Kim Hixson's Sold flyer because "the property photo runs about 3 points past the left edge of the page". It does — and so does the well it was drawn into: Sold's photo frame starts at -3.1 points by design. A created object has a new id, so it looked as though it had no prior claim on the layout, when in fact placement copies the measured frame's exact position and size. A created image is now matched to the design's frame it stands in, within two points on every side, and inherits that frame's allowance. Pushed any further than the frame it replaced, it is still reported. |
| 2026-08-14 | A line break the designer typed is not wrapping, so it does not count against a single-line field | Once text was fitted to the lines it really wraps into, New Listing was refused for a beds box "1 percent" too narrow. The design draws its counts as "4\nBedrooms" on two lines, and beds is a single-line field, so a two-line label was measured as though it had to fit across one. The single-line rule exists to stop a value WRAPPING — half an email on a second line is broken, and a name that wraps lands on the title beneath it. An explicit break the designer wrote is not that. The budget for a single-line field is now the value's own explicit lines, and a name with nowhere to go is still refused. |
| 2026-08-14 | The designer's own line break is respected by the fitter as well as by preflight | The line-budget fix was applied where preflight measures a replacement and not where `plan_fits` fits the boxes a run filled, so New Listing still refused: "the 3\nBedrooms would have to be shrunk so far it would be hard to read". Two places asked the same question and only one had been corrected. The same rule now governs both. |
| 2026-08-14 | A value's own line break never reaches a Slack sentence | That refusal read "the 3⏎Bedrooms would have to be shrunk", with a raw break inside the sentence, because the design writes its counts across two lines and the message quoted the box text verbatim. Whitespace is collapsed before a value is spoken. |
| 2026-08-15 | Gable writes in paragraphs rather than one block | Chase, reading a refusal: "She needs to say a message and have a \n so it's not just one run-on sentence. His writing needs to be not one big block." A finding, what Gable did about it, and what happens next are three different thoughts, and a reader takes them in far faster with air between them. `voice.paragraphs` joins what a message has to say with a blank line and drops the empty parts, so a caller can pass an optional note without guarding it. Every delivery note and every refusal now uses it. The house style is unchanged otherwise: still no lists, no headings, no labels — sentences, with room to breathe. |
| 2026-08-15 | A note panel with nothing to say keeps the design's own words rather than stopping | Once a note that only repeated the request type was dropped, Donald Clark's rebuild stopped to ask what the note should say — on a listing whose details column said only "Under Contract". Under Contract's panel ships reading "Ready to Buy? / DM me to find your next home.", which is a perfectly good call to action, and `replacements` already leaves it alone when the value is empty. So an empty note is not a gap, and `listing_note` is the first entry in `OPTIONAL_FIELDS`: recognised, fillable, and never a reason to pause. Every other recognised field with no value still stops, because a blank there really is a hole in the flyer. |
| 2026-08-15 | A photo well that nearly reaches the page edge is snapped to it | Kirby-Jay John's Sold flyer came back from the visual gate with "a thin white gap runs along the right edge of the photo and address panel". Sold's well is drawn 3.1 points past the left edge and 4.2 points short of the right, so a full-bleed photograph landed with a hairline of white down one side. A well within six points of an edge — under a tenth of an inch — is now extended onto it. Six points is far tighter than any deliberate inset in these designs, the smallest of which is over twenty, so a frame the designer meant to inset is untouched. This is the same class of correction as the centre crop: layout work Gable can do exactly, rather than a question. |
| 2026-08-15 | An open house answered in the thread is the one that gets printed | Gable asked Jay Hinish's listing for the open house date and time, was given "Saturday, Aug 22, 2026 1-3PM", and asked again. `open_house` has been a suppliable field all along and the answer was recorded — it was simply never read. Every other suppliable value reaches the flyer through `known`, which the research gate fills from supplied facts and lookups; the open house came straight off the form, so a stated one had nowhere to land. It is now read back the same way the reviewer's name and the shortened quote are. The form's own value is still used when nobody has answered. |
| 2026-08-15 | A question nobody has answered is the only kind still asked | Gable asked Jay Hinish's listing for its open house date and time, was given it, and asked again — three times. The answer was recorded correctly and read back into the values correctly; what kept asking was the coherence check, which reads the form's own empty column and knows nothing about what anybody supplied. A question names what it wants in Carmen's words while a supplied fact is stored under the field it fills, so `needs.internal_name` inverts `needs.readable` and `still_unanswered` drops the questions already answered before they join the batched ask. |
| 2026-08-15 | The research gate drops answered questions too, not just the first check | The first coherence check learned to skip a question somebody had answered, and Gable still asked Jay Hinish's listing for its open house — because `research_gate.resolve` re-runs `plan`, which re-runs that same check against the form's own empty column. Both entry points now filter against the supplied facts, and a step left with nothing to ask becomes a build rather than an empty question. Two places asking the same question and only one of them corrected is the second time that shape has bitten this week; the fix belongs at every seam that asks. |
| 2026-08-15 | A Slack message and the outbox row that owes it are compared with whitespace collapsed | A Sold review posted on 2026-08-14 was accepted by Slack and never confirmed. `conversations.history` returned it with its `\n` rendered as a single space, so the stored text and the accepted text could not compare equal and reconciliation answered UNKNOWN — the one verdict with no way out, since it neither confirms the row nor permits a repost. The row stayed pending for over a day, logging an error a minute. The comparison now collapses every whitespace run on both sides. The notification id in the message's own block is what identifies it; the text check only corroborates that Slack accepted what the outbox meant to say, and two outcomes differing solely in where a line wraps are the same outcome. This reached every message rather than one, because Gable now writes in paragraphs. |
| 2026-08-15 | Everything a run says belongs under that run's own announcement | The same stuck row carried no thread, and the delivery path reads no thread and no headline as "nothing has been said about this work yet" — so it tried to post the outcome as a fresh root message and bind it as the run's thread. The run had already announced itself and held a different thread, so the bind was refused on every pass. Refusing was right: a bare "I rendered it, but..." at channel level, detached from the listing it belongs to, is worse than a stuck row. A thread-less notification now adopts its run's thread before delivery, writing the thread alone and deliberately not the headline timestamp — binding a headline would mark the outcome delivered without ever saying it. |
| 2026-08-15 | A checked flyer's message names every problem, not just the first | An Open House run found two: the supplied photo showed house number 444 against an address of 5 Elphin Ct, and the date line read "Satuday", faithfully copied from what the agent typed into the form. Gable reported the photo and kept the spelling to itself, because the lead sentence comes from a judge that names one problem and that sentence was the whole message. Carmen would have replaced the photo, waited for the rebuild, and only then heard about the spelling — the exact round trip this work is removing, for something Gable already knew when it first spoke. Every problem the lead does not already mention now follows it as its own paragraph. |
| 2026-08-15 | A flyer parked in review accepts a replacement photo | The same run then refused the replacement: "This listing is not waiting for a photo, so I left the current flyer unchanged." Review is where the visual gate parks a flyer whose photograph it will not accept, so the one thing that fixes the run was the one thing the run would not take, and the only way out was starting the row over. Review means built and withheld, so there is no finished flyer to overwrite and none of the wording that guards a delivered one is needed. The resume claim now carries the exact state the upload was accepted in — read inside the guard, after the question it answers is satisfied, because satisfying one moves the run — so a run that pauses for a different reason during the source refresh still refuses a stale image. |
| 2026-08-15 | An hour range with no am or pm is a time when what is left still reads as a date | Morgan Muse's Open House flyer read "Saturday August 8, 11-1 &#124; &#124; $325,000": the whole string went into the date box because the strict time pattern requires a meridiem, so the time box was emptied and the design's own two separators stood either side of a gap. The strictness was deliberate — "Aug 8-9" is two days, not two o'clock — so a bare range is now taken as a time only when it sits at the very end and the date half still carries a day number afterwards. "Saturday August 8, 11-1" leaves "Saturday August 8" and splits; "Aug 8-9" would leave "Aug" and does not. |
| 2026-08-15 | Headroom above a cropped house is capped against the picture that survives, not the part discarded | Two flyers were refused the same afternoon for cutting off a dormer and a roof peak, from portrait uploads into wide hero frames. The top-weighted crop was already in place and still took too much: its share is a share of the DISCARDED height, so a 1080x1149 upload into a 2.14:1 hero discards 644 rows and 20% of that is 129 — a band of sky and roof. Headroom is a property of the finished picture rather than of how much was thrown away, so the offset is now also capped at 8% of the kept height. On the confirmed Dawn Rea flyer this moves the crop by two pixels, which is the point: it bites only where the source and the frame disagree sharply. |
| 2026-08-15 | A photo the run cannot use does not silence the words beside it | Gable asked Tambria Eaton's listing for its address. The reply gave the address and attached the photo, in one message, which is exactly what the batched ask trains people to do. Every message carrying a file routes to the photo path, the run was waiting for information rather than an image, so the upload was declined — and the address was discarded with it. Gable said "This listing is not waiting for a photo" and the person had answered correctly and got nothing. A declined upload now falls through to the ordinary reply path, and says nothing itself when the message had words of its own, so the answer to the question is the whole response rather than a second one. |
| 2026-08-15 | The geometric audit outranks a layout opinion it has measured false | Tambria Eaton's flyer was parked in review for "the footer slogan is clipped along the bottom edge" when the slogan sat 2.5 points inside the page, at the identical spot in the design and the built flyer. A layout-kind visual complaint is a claim about rectangles, and by the time the vision gate runs, every rectangle has already been measured against the source design. When that audit found no regression, layout-kind problems are dropped before the verdict is acted on; text, photo and placeholder kinds are untouched, because geometry cannot see a wrong digit, a stranger's face, or ink inside a box. A disproven layout claim beside a visible source-photo conflict now leaves a pure photo conflict, so the run asks for a replacement photo instead of parking. |
| 2026-08-15 | Every path that declines an upload leaves the words beside it answerable | An independent audit found the message-path fix incomplete in three places. A mention carrying a photo the run could not take discarded the sentinel and said nothing at all; a reply with two images and the asked-for address was refused before the run was even looked up, discarding the address; and the sentinel comparison itself never fired in production because the durable outbox swallows the sentence and returns empty — the sentinel is now returned in place of a message rather than compared against one. A multi-image reply whose words carry no value still gets "exactly one image", which is the informative answer there. |
| 2026-08-15 | The outbox owns a photo outcome from the moment its row exists | Delivery raising after the row was persisted left the spoken sentence intact, so the Bolt wrapper said it and the retry worker said it again within the minute. The message is cleared before delivery is attempted, not after it succeeds: a failed delivery defers to the process-lifetime worker, which is the component built to say it exactly once. |
| 2026-08-15 | A rebuild waits for the upload that is still being fitted | A photo upload satisfies its question, then spends seconds downloading and publishing before it claims the run. A text reply saying to run it again inside that window won the paused-run claim, rebuilt without the photograph, and the upload's own resume then lost and completed its ingress — dropping the photo that had just been sent. The rebuild path now checks for an open file-share ingress claim on the run and waits its turn. |
| 2026-08-15 | The bare-hours splitter counts its times with its own rule | "Aug 8, 11-1 and Aug 9, 11-1" measured its times with the meridiem pattern alone, found none, and left the first "11-1" in the date box above its own time box — the duplicate the meridiem forms were cured of, re-manifested one regex away. Bare copies are now found with the same day-number-context rule that keeps "Aug 8-9" a pair of days, identical copies are all removed, and two DIFFERENT bare times refuse to split at all, because a bare "2-4" promoted to the time box would assert itself as THE open-house time. A value that is nothing but a time no longer falls back into the date box beside itself. |
| 2026-08-15 | The lead sentence covers what it says, not a position in the list | The multi-problem fix iterated from the second problem on the assumption the lead covered the first. The list puts text problems first and the lead is usually the visual judge's sentence, so a wrong price vanished whenever the picture also drew a complaint. Deduplication is by content against the lead, never by position. |
| 2026-08-16 | Listing values route to the rebuild path however the flyer looks right now | The delivery message says "send them here and I will run it again" when nobody gave a value. A reply that took that offer — "4 beds, 3 baths, 2,650 sqft, $675,000" — was routed by the brain to `correct_field`, an in-place edit, and the post-delivery edit pause answered "Post-delivery flyer edits are paused until I can verify a separate draft." The promise and the guard contradicted each other, and the machinery to honour the promise already existed: `supply_listing_value` reopens a delivered run and rebuilds with the values. The tool boundary is now explicit — listing facts are `supply_listing_value` whatever the flyer currently shows; `correct_field` is for one wrong contact detail — and the pause's own wording now points at the rebuild instead of refusing in jargon. |
| 2026-08-16 | A note has to contain a letter or a digit to be printed | Row 110's details column holds a single "?". The dismissal list stripped " .!" and not "?", so "?" was neither empty nor a known dismissal, and Douglas White's Under Contract flyer printed a callout panel containing nothing but a question mark where the design says "Ready to Buy? / DM me to find your next home." The visual gate caught it, which is the only reason it did not ship. Enumerating punctuation is a losing game — the next agent types "..." or "!!" — so a note now has to carry at least one alphanumeric character to say anything at all. |
| 2026-08-16 | Text is judged against the siblings the designer sized with it, not only an absolute floor | Louis Smith's Open House put "2:00 to 4:00 p.m." at 9.0pt between a date and a price still at 24.2pt on the same rule. That cleared the 8-point readable floor, so it was applied, and only the visual gate noticed a caption sitting between two headlines. The floor is now also relative: a box that shrinks below 65% of the size its same-sized siblings KEPT is reported as not fitting instead of being applied. Only against siblings still standing — a design that sizes a box for the word "Email" or "Phone" means any real value to be shrunk hard, and such a box has no same-sized peers left, so it is untouched by the rule. |
| 2026-08-16 | A design box Gable would blank is asked about before the copy is made | Row 16's open house reads "7/11/2026" — a date with no time. Open House sets the date and the time in separate boxes, so the date filled, the time box was blanked, and the flyer showed the design's own two separators with a gap between them. The visual gate refused it, one whole round trip after the answer was knowable. Blanking is still the right fallback — the alternative is a previous listing's real time on somebody else's flyer — but it is a fallback, and a box this run will empty is a value the one batched ask should have covered. Preflight now reports any literal whose replacement would be empty while its field has a value, so it joins the first ask. `slides/measure.py` was split out of `preflight.py` in the same change to keep it under the line ceiling: geometry there, verdicts here. |
| 2026-08-16 | Trimming a long message keeps its paragraphs | The house rule says Gable writes in paragraphs rather than one block, and `shorten` quietly undid it for every message past 600 characters: it split sentences across the whole message on a whitespace run, which consumes a blank line, then rejoined with a single space. Found by running Carmen's four-paragraph introduction through Gable's own style gate before posting it — the gate reported the text clean and returned one wall of text with the closing sentence gone. The trim now works paragraph by paragraph, joins sentences inside a paragraph with a space and paragraphs with a blank line, and drops a paragraph that does not fit whole rather than leaving a fragment. |
| 2026-08-16 | The allowlist names people, and a person may hold more than one account | Carmen replied to Gable three times in #calvo — twice with listing answers on 2026-08-12, once with "Thanks Gable!" — and got silence every time. `GABLE_SLACK_ALLOWED_USER_IDS` held a Carmen account, but not the Calvo Consulting guest account she actually posts from, and `speaker_allowed` is the first check in both the mention and the message handler, so every word she sent was dropped before it was read. A mention would have failed identically. The rule that the list must hold "exactly two user IDs, for Carmen and Chase" then crash-looped the service when her real ID was added beside the other one, so the count is now a floor of two and a ceiling of six: one ID locks somebody out, and past a handful the list has stopped naming the people who run Gable. |
| 2026-08-16 | Being named in the channel is being spoken to | Carmen wrote "Thanks Gable!" straight into the channel and got nothing back. An ordinary message outside a Gable-owned thread was ignored by design — a sound rule for a channel Gable shares with people talking to each other, and indistinguishable from a snub when the message is addressed to Gable by name. A top-level message that names Gable, or mentions its user id, now takes the same path a mention takes and becomes an owned thread so the follow-ups work. A message that does not name Gable is still ignored, because the channel carries Carmen and Chase talking to each other. The name is checked against a short list of roof words first: this is a real-estate channel, and a gable roof is not somebody addressing Gable. |
| 2026-08-16 | A trailing country is the one thing the address tidier removes | "225 N Wycombe Ave Upper Darby, PA 19082 United States" arrived from address autocomplete. Every shape check requires the text to end at the ZIP, so a complete address failed validation and Gable asked Carmen to retype one the form already held — the same failure the lower-case-state bug caused, one regex away. The country is stripped in `listings/address.tidy`, the canonical rule set, so the existing missing-comma machinery then completes the split. It is anchored at the end, so a street carrying those letters is untouched, and it is the only removal the tidier makes: no design prints a country. |
| 2026-08-16 | A preflight blocker is asked with the photo, never one step before it | Lina Mariner's New Listing stopped on a missing headshot and said only that. It was equally certain it would need the property photograph, so Carmen would have added the headshot, said run again, been asked for the photo, and answered a second time — two round trips where the batched ask exists precisely to make one enough. Blockers now join that one message: every one of them, because reporting only the first hides the second until the first is fixed. The photograph is named explicitly when a blocker sits above it, since "Can you send me the image?" under a sentence about a headshot names two different images and reads as one. `needs_info` joins the states a photo upload may resume, because a run that asks for a photograph has to be able to take it. |
| 2026-08-17 | A flyer that exists is delivered, whatever the vision pass thinks of it | Carmen supplied every value and the photograph for 5556 Dolores Ave and 210 S Monastery Ave, and got "I rendered it, the main property photo is cropped through the top roofline and chimney. I've not sent it as finished." Both flyers existed. She received a description of a file she could not open, which leaves her nothing to act on and every reason to build it by hand — the exact twenty minutes Gable exists to remove. Withholding also assumes Gable's judgement of her own photograph beats hers, which is backwards: she reviews every post before a client sees it. So a built flyer is now always delivered, with every finding stated directly under the link and an offer to redo it from another photograph. The `needs_replacement_photo` branch is gone with it — the finding and the offer say the same thing without holding the file hostage. What still holds a run is a flyer that does not exist or could not be read back at all. Chase's call, twice stated. `questions.request_replacement_photo` now has no callers. |
| 2026-08-17 | A photo the wrong shape for the frame is contained, not cut in half | `assess` measured `crop_loss` from the first commit and used it to choose nothing: the only thing that avoided a crop was a source too SMALL to enlarge. So a portrait upload was cropped as hard as filling the frame demanded. Measured on two real flyers: a 1320x1918 photograph into the Under Contract hero, 809x420pt at 1.924:1, lost 64% of itself, and a 1000x1080 into the Sold hero at 1.923:1 lost 52%. Both kept the house and lost the entire front garden and the bottom of the porch. Chase: "the image is so cropped in the flyer". Past 45% the whole photograph is now kept over a blurred, darkened copy of itself — the same source-only path the small-source case already used, and the one CLAUDE.md §8 already sanctions. The line is above 38%, the worst an ordinary landscape source loses (4:3 into the widest 2.14:1 hero), so every normal photo still fills its frame exactly as before. The two paths stay distinguishable because only a genuinely small original earns "send a higher-quality version" — a tall photograph is not a low-quality one. |
| 2026-08-17 | Reverses the row above: Gable answers a mention in the channel, not its own name | Matching the word "gable" in a top-level message meant Chase telling Carmen "Gable will build this one" got an answer from Gable. Talking about Gable is not talking to Gable, and a bot that joins a conversation it was only mentioned in is worse than one that misses a thank-you. The rule is now Chase's: in the channel Gable replies only to a real `@Gable` mention, and inside a thread it owns no mention is needed, because a thread is already a conversation with it and making Carmen type the at-sign on every reply is its own round-trip tax. "Hey Carmen" and an @carmen mention were already ignored and stay ignored. The name-matching predicate and its roof-word exception are deleted rather than left unused. |
| 2026-08-17 | The experience is measured, not inferred from a status | Every defect on 2026-08-16 and 17 was found by Chase reading a Slack thread; the database showed `delivered` either way, so a listing that took six replies looked identical to one that took none. `tools/audit_experience.py` counts transitions into a paused state per submission — one line per listing, worst first, non-zero exit when any delivered listing needed more than one ask. `tests/test_photo_frame_matrix.py` guards the other blind spot: nothing had ever put a tall photograph into a wide frame and checked what survived. It runs all 31 distinct measured hero frames, whose aspects span 0.478 to 2.726, against nine real photo shapes, and asserts both that nothing is quietly gutted and that an ordinary landscape photo still fills a landscape frame — over-containing would be its own regression. |
| 2026-08-17 | An incomplete address is printed, not asked about twice | Lina Mariner's form gave "5556 Dolores Ave 21227" — a street and a ZIP, no city. That rode the one batched ask correctly, beside the beds, baths, square footage and price. She answered with three of them, and Gable came straight back with "I still need the address", which is the exact second round the batched ask exists to remove. Two gates were doing it: `needs_a_whole_address` in the runner had no released-blanks check, unlike every other value, and `manifest.validate` refuses any present-but-malformed value forever on the grounds that printing one states something untrue. That reasoning is right for a price and wrong here — a street and a ZIP with no city is INCOMPLETE, not false. `Problem.releasable` now marks a value that is imperfect and still true, so after the ask it is printed as supplied. An address that identifies no place at all ("call me", "TBD") is not releasable and keeps asking. |
| 2026-08-17 | A question has to be worth its round trip | Gable asked "are you saying Lina's headshot is now in the Head Shots folder and you want me to run again?" of somebody who had just said exactly that, and then asked her to repeat it. The conversation prompt told the model to confirm anything ambiguous without ever weighing what a confirmation costs. It now says the cost out loud: never confirm something the person just told you or something the listing's paused state already answers, because rechecking is cheap and asking is not — a run waiting on a missing headshot, sent a headshot, should reload and check rather than ask. `questions.request_replacement_photo`, `replacement_message`, `PHOTO_REPLACEMENT_QUESTION` and `PHOTO_REPLACEMENT_WAITING` are deleted in the same change: the 2026-08-17 delivery policy left them with no callers, and a dead ask is the easiest kind to accidentally revive. |
| 2026-08-17 | The message that delivers a flyer is a report, and is not trimmed like a reply | A 535-character delivery message lost two whole paragraphs at the 600-character ceiling, one of them the note naming the fields nobody supplied — the single most actionable thing in it. `MAX_REPLY_CHARS` is right for a conversational reply, which should be short because it was written short; a delivery carries the link, anything a check noticed, what is missing, and what Gable resized, and every one of those is a fact Carmen acts on. `MAX_DELIVERY_CHARS` is 1400 for that one message, still bounded. The paragraph order is also reversed: what she must act on now precedes what Gable already handled, so if a trim ever does bite it takes the informational tail rather than the missing-fields note. |
| 2026-08-17 | An address field naming two properties asks rather than prints | Checking the incomplete-address rule against all 330 real submitted addresses found one holding two listings separated by a line break. Collapsed, it read "5111 Hanover Pike Manchester, MD 21102 75 S Ralph Street Westminstermd, 21157" — a single string naming neither house, which fails the whole-address shape and would therefore have been printed under the incomplete-but-true rule added the same day. It is not incomplete; it is false about both properties, and a wrong address on a real listing is the worst output this system has. More than one ZIP in the field now means more than one property, which blocks and is never releasable. 297 of the 330 are complete, 10 are incomplete and printable, and 23 correctly keep asking. |
| 2026-08-17 | A Reel or a Story is not a graphic, and Gable silently builds nothing for it | The social-media content type was read as out of scope from the first commit, on the reasoning that Gable makes flyers rather than reels. That was true of what Gable *builds* and false about what it must *read*: the column decides whether a graphic is wanted at all, and without it every Reel would have become a flyer. Carmen settled it — "only the static posts are graphics. The rest are either videos or have animation" — and it is not a rare branch. Of the 112 rows on the live tab, 55 say `Static Instagram/Facebook Post`, 31 `Instagram Reel`, 16 `Static Instagram Post`, 8 `Instagram Story` and one `Instagram reel` in lower case, so 40 of 112 would have been wasted renders in front of Carmen. Skips are recorded terminally in `runs` and nothing is posted about them, per Chase: at better than a third of volume a notice per row is noise, and the batch summary is still a message. Matching is exact and case-folded, so an unknown or blank value **builds** — an extra design costs a glance, a silent skip is a request that vanishes with nobody told. The content type is also persisted (schema v13) so a submission reloaded from the database equals the one written; without it a resumed thread would read every stored row as "build". |
| 2026-08-17 | A skip is one transaction, because a half-written one is worse than no skip | Found by writing the test rather than by it failing in production: `_drop_animated` opened the run and set it terminal in two autocommit statements, so any failure between them — a locked database, a restart — left a `pending` run behind. That is the worst available state. `has_been_handled` already reports the row done so nothing reconsiders it; `pending` is an ACTIVE status so `start_run` refuses another attempt; and the next startup's `recover_interrupted_runs` fails it and owes Carmen a Slack notice about a Reel Gable had just promised to say nothing about. The pair now runs inside `BEGIN IMMEDIATE`, the same wrapping `adopt_backfill` uses for the same two calls, and the poller's connection is thread-owned so it does not contend with a Slack handler. The docstring claiming a failed skip is "retried next pass" was also only true when `start_run` itself failed; both halves are tested now. |
| 2026-08-19 | A name is compared by its words, so a credential written into one still finds the official profile | Gable told Carmen to "add REALTOR to Caleb's request", she wrote the agent name as "Caleb Olawuyi, Realtor", and the next run then refused with "no exact profile for this agent" — Gable's own instruction broke its own lookup. WordPress returns nothing for the whole branded string, and the two-word retry found the right pages but discarded them, because `_is_branded_form_of` tested a string prefix with a trailing space and read the second word as "olawuyi,". Names are now compared as word lists with edge punctuation stripped, which keeps every documented refusal — "Bob" still never stands in for "Bobby", a lone first name still cannot nominate, and "Caleb Olawuyi Open Houses" is still not a branded form of "Caleb Olawuyi, Realtor" — while a comma stops being part of a surname. Punctuation inside a word is preserved so "O'Brien" stays distinct from "OBrien". Verified live against cornerhouserealty.com: both "Caleb Olawuyi" and "Caleb Olawuyi, Realtor" now resolve to the same profile. |
| 2026-08-19 | A pause names a place that can actually fix it, or it names none | The missing-credential pause reused the generic contact remedy — "Correct the request or Agents Contact Information" — and that remedy is false for a credential. `validate_contact` accepts a title only from the official profile, and the roster workbook has no title column to hold one, so both named places are unreachable by construction. Carmen spent four messages obeying it: she answered "Realtor" in the thread, filed Caleb in Agents Contact Information, appended the credential to his name on the request, and got the same refusal each time, the last one worse than the first. The credential pause now says every contact detail is correct, that the profile's job-title field is empty, that telling Gable or filing it cannot reach him, and that the profile is where it has to change. The brain carries the same rule so it cannot re-invent the impossible remedy in its own words. A message naming a fix that cannot work costs more than one naming no fix at all. |
| 2026-08-19 | Owning a thread is not the same as being spoken to | Chase wrote "@Carmen one sec let me look at this" in a Gable listing thread and Gable answered "Take your time, Chase"; it had already answered Carmen's "@Chase? I'm not sure what to do here" in the same thread. Ownership was the only test an ordinary reply faced, and inside an owned thread every reply was conversation for Gable — which is right for the unaddressed replies Carmen sends constantly, and wrong the moment two people use the thread to talk to each other. A reply that mentions at least one user and not Gable is now ignored. Naming Gable alongside someone else still routes to Gable, a reply naming nobody is unchanged, and a shared photo is still taken whoever the caption addressed — dropping a photo silently would cost more than a stray sentence. |
| 2026-08-19 | **REVERSES "never infer a credential": a blank profile job title falls back to one configured brokerage-wide credential** | Caleb Olawuyi's Under Contract flyer could not be built at all, because credentials were readable only from an agent's own profile and his states none — verified live, an empty `cbl__widget--job_title`. The original rule was written against Gable *guessing* that somebody is a REALTOR because they sell houses, and that guess is still forbidden. Chase confirmed that all 38 agents on the roster hold the credential, which makes `GABLE_DEFAULT_AGENT_CREDENTIAL` a stated fact about the brokerage rather than an inference about a person, and he asked for it rather than for the website to be edited per agent. Four things keep it honest: a profile that states a title always wins, so nothing overrides an agent's own record; the fallback applies only to the title, never to a name, email, or phone; provenance distinguishes `official_website` from `brokerage_default` so an audit can find every flyer that leaned on it; and setting the variable empty restores the old refusal exactly. The credential is still never invented per person — there is one value, and it is the same for everyone. |
| 2026-08-19 | A request name carrying branding matches its filed roster row, and the filed name is what prints | Adding Caleb to Agents Contact Information would have failed on a name conflict, because his request still reads "Caleb Olawuyi, Realtor" after Carmen followed Gable's instruction to put the credential there. The workbook comparison only tolerated an *incomplete* filed row, so a complete row plus a branded request was a hard refusal — while the official-profile comparison had allowed exactly that shape since Bobby Carr. The two now agree: a submitted name that is the filed name plus a suffix is the same person, the email still has to match exactly, and `name_ready` is satisfied by the filed row, so the roster's "Caleb Olawuyi" prints and the credential Carmen typed into the request never reaches the flyer. A filed name that genuinely disagrees is still a conflict. |
| 2026-08-19 | `RunResult` moved from `runner.py` to `run_reporting.py`, re-exported | `runner.py` sat at exactly 800 lines, so wiring one setting through it broke the hard ceiling. The type describes what a run did in the words Gable would use, which is what `run_reporting` is for, and moving it took the file to 796 without touching the sequence. `runner` re-exports the name so `from gable.pipeline.runner import RunResult` keeps working for `slackapp/photos.py` and the tests. This is a stopgap and should be read as one: `_sequence` is a single 489-line method, and that — not the file length — is the design signal worth acting on. |
| 2026-08-19 | An address that cannot be printed is named, not reported as missing | Gable opens every listing thread by naming the property, so Caleb Olawuyi's thread announced "4216 Norfolk Avenue, Baltimore 21216" and then said "I still need the address" — which Chase read as a mistake, correctly. The check is sound: that address carries no state, the design prints street, city, state and ZIP, and `needs_a_whole_address` fires only when an address IS supplied and is the wrong shape. Saying it is missing contradicts the message above it and sends Carmen looking for something she already sent, which is the same defect as the credential pause earlier the same day — a right refusal wearing a wrong sentence. `needs.incomplete_address` now echoes the address in hand and names the fault: no state when no state token is present, out of order otherwise. Both branches are true, and neither claims to have nothing. |
| 2026-08-19 | The brokerage credential is configured in title case, because the case rule only ever capitalises | Carmen reported the spacing "coming through weird". Under Contract draws its credential as "Realtor" and `fields._MATCH_PLACEHOLDER_CASE` upper-cases the value only when the placeholder is upper-case — it never lowers one. The default shipped as `REALTOR`, so a wider all-capitals word went into a box drawn for title case, no longer fitted, and autofit took it from 23.95pt to 18.42pt. The design's own type size is the thing being protected, so the default is now `Realtor` and New Listing still gets its capitals from the placeholder rule. Found by comparing the delivered flyer's rendered sizes against the source design rather than by reading the code. |
| 2026-08-19 | A setting whose documented off-switch is emptying it needs a reader that can tell empty from unset | `.env.example` and `AGENTS.md` both promised that clearing `GABLE_DEFAULT_AGENT_CREDENTIAL` restores the old refusal. It did not: `str_value` reads blank as unset and returns the default, which is right for every setting that must always carry a value and wrong for one an operator turns off. `switchable_str_value` distinguishes a variable that is present and empty from one that is absent. Caught by writing the test for the escape hatch the documentation had already promised. |
| 2026-08-19 | An unprompted template re-read that finds nothing says nothing | Carmen edited three designs in four minutes and got three Slack notifications reporting that Gable had looked and found no problem — in #calvo, where real listing threads arrive. Nobody asked it to look, and "nothing happened" is not news; it is noise in the one channel that must stay worth reading. A clean automatic re-read now records its audit and stays silent. Everything else still speaks: any blocker, any capacity warning, any failed or inconclusive visual inspection, a brand-new design filed for the first time, and every re-check somebody explicitly asked for — because a question Carmen asks must always be answered. |
| 2026-08-19 | The request's email identifies the submitter, so the agent is identified by name against the roster | Two requests stopped within two minutes of each other, both submitted by one person on behalf of other agents: the address on each was hers, so it proved nothing about either agent. Tonette Campbell's filed row and her official profile agreed exactly on email, phone and credential — the only thing wrong was that Gable was looking her up by somebody else's address. The rule now falls back: when the submitted email is not the named agent's, find exactly one roster row with that name and treat its email as the agent's. The guard is unchanged in substance — the profile still has to corroborate, one row or none decides it, and a same-named page on the website is never trusted on its own, because choosing between two people with one name is choosing whose phone number a client rings. An agent the roster does not carry and whose profile proves nothing now gets told exactly that instead of "the official profile does not show the submitted email address", which named a fact Carmen could do nothing with. `ProfileLookup.found_but_unproven` carries that distinction as a field rather than as message text. |
| 2026-08-19 | **Narrows "a placeholder is left showing": a stat slot is blanked when nobody supplies it** | Mike Nugent's New Listing flyer read "3 Bathrooms". Carmen supplied beds, square footage and price; New Listing is drawn with Kelsey Mahon's sample listing at 4 bed / 3 bath / 2,430 sq ft / $350,000, so the bathrooms slot kept the design's three and no reader could tell it from the numbers beside it. Gable named the unfilled field in Slack exactly as the rule requires, which is a sentence against a number sitting in a stat row on a client-facing flyer. The rule was written for placeholders that read as placeholders — "PROPERTY ADDRESS" cannot be mistaken for an address — and a bare number is not one. An audit of the 31 flyers delivered since going live found three carrying a slot that never changed while every slot beside it did: Andy Jang's bedrooms, Lina Mariner's asking **price**, and Mike Nugent's bathrooms. Beds, baths, square footage and price are now emptied when unfilled, so the icon and its label remain and the flyer reads as incomplete rather than as confidently wrong. Delivery is still not blocked and Gable still names what it left out. |
| 2026-08-19 | Two fields resolving to one placeholder literal refuses the design instead of silently filling both | Found while checking the blanking change rather than in production. `fields.replacements` is keyed by the literal, so a design drawn with three bedrooms and three bathrooms gives both fields the literal `"3"`: the second entry overwrites the first, and `replace_text` then swaps every occurrence on the slide. A four-bed two-bath listing would render as two and two, with nothing to show it had happened, and the standalone-literal guard cannot catch it because both occurrences genuinely are standalone. None of the six designs collides today — verified against all six — but Carmen redraws these, and 3/3 is an ordinary house. `preflight.analyze` now refuses an ambiguous design and names the two sections, which is the same treatment a duplicate template name already gets. |
| 2026-08-19 | `fields.py` split: the placeholder data moved to `slides/placeholders.py` | The file reached 823 lines adding the ambiguity guard. The seam was already there and is the honest one: half the file is what the designs literally contain — sample agents, the real Corner House contact details left in the artwork, and the patterns that recognise a fillable slot — and half is the logic that reads it. 351 lines moved unchanged, leaving 484 and 368. Nothing outside `fields.py` imported the tables, so no caller changed. |
| 2026-08-19 | Gable subscribes to `file_shared`, because a message can arrive without the file it carried | Caleb Olawuyi's photo never reached Gable: Carmen uploaded it with a caption, Slack delivered a `message` event with no `files` array, and Gable asked her for a photograph she had already sent. Reading the same message back through the API showed the file attached, so the upload was real and the event was simply early — Slack's current flow can post the message first and attach the file a moment later. Carmen's next upload worked, which is what made it look intermittent rather than broken. `files:read` was already granted; only the event subscription was missing, and it was added to the live app on 2026-08-19. `shared_file_event` reshapes the notice into the message-shaped event the existing handoff already reads, so one code path places a photograph however Slack chose to announce it, and the same ownership rule applies. The photo identity moved from the message timestamp to the file id, which is the one identifier both routes agree on, so an upload announced twice is placed once. |
| 2026-08-19 | The counts an agent wrote in their own post details fill a bed, bath or square-footage slot nothing else supplied | Both 1921 Lincoln Ave requests carried `3Bed/2 Bath` in the form's details field, and both flyers printed a bathroom count from their design's sample content instead — three on the New Listing, five on the Open House, for the same three-bed two-bath house. Gable had the right answer in hand and never read it. `listings/details.py` now reads bedrooms, bathrooms and square footage out of that free text, and only fills what `known` left empty, so a researched or human-stated value still wins and nothing already established is overridden. Two different counts for one field — a main house and a guest suite — read as unstated rather than resolved, because choosing would put a number on a client's flyer that nobody wrote. The price is deliberately excluded: a list price, a Sold closing price and a Price Reduction's new price are different values with different rules in `intake`, and lifting a number out of prose would cut across them. Verified against both real submissions before shipping. |
| 2026-08-20 | Whatever Gable asks for in a message, Gable can receive — the ask is recorded on the run, not inferred from its status | Effie Fafaleos' Open House posted one message asking Carmen to widen the design *and* to send the property photo, parked in `needs_template`, and then answered her upload with "This listing is not waiting for a photo, so I left the current flyer unchanged." The only exit was starting the row over. One `status` column was being asked to carry two simultaneous waits, and the blocker won because widening a design is the part only a person can do outside Slack. Flipping the status to `needs_photo` instead would have broken the reply that says the design is fixed, which routes on `needs_template`. So the photograph became its own fact: `runs.awaiting_photo` (schema v14) records whether the outgoing ask included it, and an upload is accepted in **any** paused state that asked. Safe in all of them for the reason `needs_review` was already safe — none has sent a flyer, so there is nothing to overwrite — and a paused run that asked for nothing still declines a stray upload. The same signal closed a second hole: `build_with_blank_fields` released a run that was owed its photo, and would have built a flyer with no property photograph on it. This is the third instance of one class in two weeks (the credential remedy, the address ask, this), so the regression test is written against every paused state rather than the two that were reported. |
| 2026-08-20 | A request naming three open houses builds the first and names the rest, instead of asking for a wider template | The same run's blocker read "The open house would need about 227 percent more room... Widen that section" -- a remedy that cannot work. The value was "Friday, Aug. 21 4pm to 6pm, Sat. Aug. 22 10am to 12pm, Sun, Aug. 23 11am to 1pm", and the measured Open House design draws one 245pt date box and one 72pt time box, both single-line; no width holds three different hours in one time box. Worse, the width check was the only thing preventing a bad flyer: `_open_house_part` applied its "two different times are two facts" rule to bare forms only, so the meridiem path split this into "4pm to 6pm" beneath a date box still reading the other two. A wider box would have shipped that. The rule now applies however the times are written. What happens next was Chase's call the same day -- **Gable cannot get stuck; it produces the flyer no matter what** -- so this does not block and does not ask first: the first occasion goes on the flyer and the delivery message names what was left off and offers to rebuild for another date. That is the 2026-08-17 rule again, that a built flyer beats a withheld one plus a description of it. Two days at the same hours stay one open house, and a design setting date and time in ONE box takes the whole request untouched -- only a separate time box forces a choice. |
| 2026-08-20 | `runner.py` split again: question and outcome delivery moved to `run_speech.py` | The `awaiting_photo` write took the file to 811, past the hard ceiling. `_ask` and `_outcome` were both thin wrappers over the durable outbox, and the seam is real rather than arithmetic: everything moved is about getting an exact sentence into the outbox and then into Slack, and nothing moved decides what the sentence should be. Two user-visible sentences that were inline in the runner — the attempt-limit message and the failed-step message — became named constants in `run_reporting.py` beside the others, so the words Gable can say are readable in one place. The 2026-08-19 note still stands: `_sequence` is the design signal, not the line count. |
| 2026-08-20 | A review rejected the first `awaiting_photo` fix, and it was right | The column was added and wired into three of the eight places that answer "is this run waiting for a photo?", and two omissions turned the fix into a new dead end at a different door. `_needs_fresh_photo_upload` gated two different questions -- "may I build without a photo" (answer: no) and "may I re-measure the design" (answer: always yes) -- so a `needs_template` run owed a photo would have refused *"tell me to check the updated template again"*, the exact instruction its own blocker gives. It is now two predicates. `resume_claim.PHOTO_RESUME_STATES` still listed three states by hand, so the real claim took the branch outside `run_question_guard` and, during the acknowledgement gap, lost the upload and told Carmen the listing was "already being rechecked" -- which was false. It is now derived from `store.PAUSED`, and `photo_store._pending_photo_row` matches the recorded ask rather than only `target_status = 'needs_photo'`. Startup recovery released an abandoned photo claim in silence for every paused state but `needs_photo`, so a crash mid-download lost Carmen's photo with nothing said. `tools/run_row.py` never cleared the flag. `set_awaiting_photo` wrote no `run_events` row, against the module's own contract. All fixed. The finding that matters most for the next agent: **the parametrised test passed the whole time because `tests/photo_support.FakeRunner` called `store.claim_run_for_photo` directly, bypassing `resume_claim.claim_for_resume` -- which is where the paused-state decision actually lives.** A green suite proved the store accepted the photo while production took a different branch and dropped it. The fake now routes through the real claim. A test double that skips the layer under test is worse than no test, because it reports the opposite of the truth. |
| 2026-08-20 | `preflight.py` split: design certification moved to `slides/certification.py` | The open-house advisory took the file past 800. The seam is the job rather than the arithmetic: certification answers "is this design safe to use at all" with no listing in hand and only ever advises, while `analyze` answers "does THIS listing fit THIS design" against measured values and blocks. `certify` and `TEMPLATE_CAPACITY_CHARS` are re-exported from `preflight`, so no caller changed. The import is at the foot of `preflight` because certification layers on top of structural analysis and calls `analyze` itself. |
| 2026-08-20 | An open-house time is written the way the design writes it | Effie Fafaleos' delivered flyer printed "6pm" across "3 BATHS". The supplied time was "4pm to 6pm"; the Open House time box is 72pt wide and holds its own placeholder "2-4PM" at 24pt, so the longhand form wrapped to three lines and overflowed downward into the stats row. Gable measured that correctly and said so -- "the 4pm to 6pm would have to be shrunk so far it would be hard to read" -- and delivered anyway, which is the right call for a flyer and the wrong one for a value it could simply have written compactly. `compact_time` rewrites a plain range into the designs' own idiom: a shared meridiem is stated once at the end, so "4pm to 6pm" becomes "4-6PM", the same five characters as the placeholder it replaces. Two different meridiems keep both, because "10AM-12PM" cannot lose either half without changing what it says, and anything that is not a plain range is left alone. |
| 2026-08-20 | The delivery note says a stat slot was left EMPTY, because that is what the flyer shows | Effie Fafaleos' finished flyer had a blank where the price goes and a message underneath reading "Nobody gave me the price, so the design's own placeholder is still there. Type over it." The 2026-08-19 decision emptied beds, baths, square footage and price when nobody supplies them -- a sample "3 Bathrooms" reads as this house's bathroom count -- and this sentence was never updated, so it sent Carmen looking for a placeholder her flyer does not have. That is the same defect as the credential remedy and the address ask: a right behaviour wearing a wrong sentence. `blank_note` now splits the two cases and derives the emptied set from `fields.BLANK_WHEN_UNFILLED` itself, so the words and the filler cannot drift apart again. Fields that genuinely keep a placeholder still say so. |
| 2026-08-20 | A `file_shared` notice recovers the words that were sent with the file | `shared_file_event` shaped Slack's file notice into a message-shaped event with `text: ""`, because a `file_shared` notice names the file rather than the message. The caption is load-bearing twice: values stated beside a photo are recorded from it, and a delivered flyer accepts a replacement image only when the words ask for one. So "here is a better angle, run it again" worked when Slack announced the upload as a message and silently did not when Slack announced it as a file share -- the same upload, two outcomes, decided by a race Gable does not control. One `conversations.replies` call recovers it, and a lookup failure still returns "", which is exactly what this route supplied before, so nothing regresses. |
| 2026-08-20 | `runs.photo_url` is not the question "does this run have a photograph" | The new "I have the property photo." sentence read that column directly, and it survives a refusal as audit evidence for the image that was rejected rather than as permission to reuse it -- `runtime.py` says exactly that in a comment three lines from where the reuse happens. So a run parked because the visual gate refused its photo would have opened its next message by claiming to hold one. `store.photo_was_rejected` asks the run's own event log instead, and `Needs.note_photo` makes the three states explicit: outstanding, held, or refused. A refused photo is neither asked for again here nor claimed -- the check that refused it owns that sentence. |
| 2026-08-20 | The one batched ask is a report, and what it commits to is recorded from the words that actually went out | Verified against real code: a listing with three design blockers plus the photo and its values produces a 1004-character ask, and `MAX_REPLY_CHARS` cut it to 548 -- losing BOTH "Separately, can you send me the property photo?" and the sentence that makes silence a usable answer. The run meanwhile recorded that it had asked for a photograph and approved building with those values blank. Carmen would have seen three paragraphs about template widths, nothing she could send, and a listing that then waited on her forever. That is the 2026-08-17 delivery-message decision again, one message earlier: a report is not trimmed like a reply, so `MAX_ASK_CHARS` is the delivery ceiling. The structural half matters more than the number. `run_speech.record_the_ask` builds the message ONCE and records from that text: blanks are released only if the releasing sentence survived, and a photo ask the message does not carry is logged as an error rather than silently believed. State that describes a message must be derived from the message, not from the intention behind it. |
| 2026-08-20 | A price a PERSON stated reaches the flyer on every request type | `intake.price_note` offers "If you give me the price I can add it" on a Sold listing, and the answer could not arrive. Carmen's reply IS stored -- the model files it as `list_price` and `remember_supplied_fact` accepts it -- but `run_values` reads that column only when `accepts_public_list_price`, which is false for Sold and Price Reduction. So the value was recorded and then discarded, the question came back, and the listing could not finish: an unfulfillable offer, the same class as the credential remedy. The rule it was protecting is still right and unchanged: a publicly SCRAPED list price is not a closing price or a reduced price, and research stays refused for both. A person stating the price is a different fact with different authority. The form column still wins; only research is overridden. |
| 2026-08-20 | **Extends "a plausible placeholder is blanked": an unanswered open house is emptied, not left as a previous listing's real date** | The 2026-08-19 rule blanked beds, baths, square footage and price because a bare "3" cannot be told from a real bathroom count. `open_house` qualified by the same words and was missed, and it is the worst member of the set: the Open House design ships "Sunday, Aug 2, 2026" and "2-4PM" -- a real previous open house at 5066 Winesap Way. Left showing it does not read as a gap, it reads as this house being open on a specific afternoon, and a date is acted on by driving somewhere, where a wrong bathroom count is checked by anyone who visits. Found by auditing `joins_one_ask` after a review flagged it: an open house with no date, and a price reduction with no new price, both ride the batched ask because any ABSENT value may join it. The comment above `BATCHABLE_CONTRADICTIONS` claimed the price reduction did not and was wrong about its own code -- corrected in the same change. What makes the batching safe is the blanking rule, not the batching rule, so the fix belongs there. |
| 2026-08-20 | A photo the visual check refused invites its own replacement, without magic words | `Verdict.needs_replacement_photo` was computed and read by nothing -- a documented field advertising a capability that did not exist. It means the visual pass concluded another photograph is the WHOLE remedy, which is the one case where a replacement is certainly wanted, and it was the case that was hardest to send: a delivered run accepted a new image only alongside an exact phrase from an eleven-string list, and the `file_shared` route carries no text at all by construction. Wiring it does not reverse the 2026-08-17 rule -- the flyer still goes out, nothing is withheld. It sets `awaiting_photo` on the delivered run, and the ingress accepts a caption-less upload only when that flag is set. A stray image dropped into an ordinary delivered thread still needs the words, because the general "send another if you want it framed differently" invitation does not set it. |
| 2026-08-20 | Two images, two destinations, said out loud | When a design has no headshot on file, the batched ask names two images at once: one to add to a Drive folder, one to send to the thread. Neither sentence said WHICH place, so an image dropped in the thread while a headshot was outstanding was genuinely ambiguous -- and Gable takes it as the property photograph, which is how a face ends up where the house goes. The headshot blocker now says "Add that image to the Head Shots folder rather than to this thread" and the photo ask says "can you send me the property photo here?". Chosen over refusing the upload, which recreates the dead end this whole day was about, and over asking which image it is, which costs a round trip on every run with a missing headshot to prevent a mistake that is visible and self-correcting the moment the flyer is seen. If it still happens, the harder guard is the next step. |
| 2026-08-20 | Three smaller untruths, from the same sweep | `"This listing is not waiting for a photo, so I left the current flyer unchanged"` was said to runs that had failed or been skipped and had no flyer at all, which reads as Gable having quietly built something it will not show; those now hear that there is no flyer yet. The conversational model was told `A human-supplied hero photo is attached to this run` from `photo_url` alone, which survives a refusal as audit evidence -- so it was invited to argue with Gable's own outstanding request for a replacement; it is now told the difference, and told when a run is owed its photograph at all. And `question_store` decided a run's target state by comparing the message text to a literal typed inline, so rewording that sentence for voice would have silently stopped the run moving to `needs_photo`, with nothing to point at. The words are one named constant now. |
| 2026-08-20 | Two more sentences that contradicted the run they described | `replace_photo` answered "This listing is already waiting on something else" on a run whose status was `needs_photo` -- which is precisely the state somebody asks to replace a photo from, and precisely the thing it was waiting on. It now names the upload instead, which is also the whole remedy: the ordinary file handoff takes a photograph in any paused state that asked for one, so this tool has nothing else to do. And "what is this waiting for" answered a `needs_template` run with the blocker alone, hiding the photo request made in the same message. Both are the class this whole day was about: a right behaviour wearing a wrong sentence. |
| 2026-08-20 | The reply shortcuts read the fact, not the status, and share one string with the writer | `intents` decided whether a short reply was a photo-wait acknowledgement by parsing `Run status:` alone, so a run owed a widened design AND a photograph -- parked in `needs_template` -- stopped recognising "yes" and "run it again". It now reads the context line that says the run is owed a photo, and `RunResult.needs_a_human` derives from `store.PAUSED` instead of listing four states by hand. The sentence itself is one constant, defined in `intents` (the module with no dependencies) and imported by `context`, so the words that get WRITTEN and the words that get READ cannot drift. Two copies would fail silently with nothing to point at, which is the same shape as a run state keyed off a message literal -- fixed the same day, two files apart. Hand-rolled copies of `store.PAUSED` are how the whole photo bug propagated; that is the general lesson, not a detail of this one. |
| 2026-08-21 | A state the submission wrote out is a state, and the address tidier writes it as the code | Deborah Manarin's Sold arrived as `2519 Ann Arbor Lane Bowie Maryland 20716`. Every check downstream reads a state as a two-letter postal code -- `manifest.ADDRESS_SHAPE` requires `[A-Z]{2}`, `address.tidy` gave the comma only to `STATE_CODES`, and `needs.incomplete_address` tested the same set -- so Gable told Carmen "I have this listing at 2519 Ann Arbor Lane, Bowie Maryland 20716, but it has no state", printing the state inside the sentence denying it. `tidy` now folds the written-out name into its code, which says what the agent said in the form every design prints. Restricted to the name sitting where the state belongs -- immediately before the closing ZIP, or at the end -- and skipped entirely when a code is already present, because every state name is also a street or a town: Maryland Avenue runs through Baltimore, Georgia Avenue through Silver Spring, and California, MD is a real town in St Mary's County. This is the only token the tidier rewrites; nothing is still added or reordered. Extends the 2026-08-19 row rather than reversing it: the fault is still named and the address still echoed, and `incomplete_address` gained a "no ZIP code" branch, because "not in the order the design prints" sent somebody hunting a formatting fault in an address missing five digits. |
| 2026-08-21 | An address stated with the photo is filed against its submission, and the build reads the corrected row | The same thread, one message later. Carmen sent "2519 Ann Arbor Lane / Bowie, MD 20716" with the photograph attached -- a whole, printable address -- and Gable asked for the address again. The 2026-08-14 caption path did read it and the model did extract it; `runtime.record_photo_caption` then called `answers.record_stated` without the `response_row_id`, so the one value that needs it was refused. `record_stated` logged "a stated address arrived with no submission to attach it to" and dropped it, exactly as designed: an address is which property this is, not a fact about one, so it is stored over the submission and has nothing to key on without the id. The caption hook now takes the submission, and the handoff reloads the row before resuming -- the same reload `supplied.py` does for the same reason -- so a corrected address in a caption is the address the flyer is built from. The signature carries the id rather than defaulting it, so the next caller cannot omit it silently. `slackapp/uploads.py` was split out of `photos.py` in the same change to stay under the 800-line ceiling: the Slack download boundary, and nothing else. |
| 2026-08-26 | **Reverses half of the source-visual gate: a design refused only on how it LOOKS no longer stops a new listing** | d613511 settled on 2026-08-14 that the open-house tag on New Listing with Open House hangs off the page on purpose, and stopped a listing REBUILD re-certifying a design's looks. The new-listing gate was never given the same exemption, and `template_clearance` could not have applied it anyway: it read `status` alone and had no way to tell a structural fault from a judgement about artwork. So on 2026-08-26 Brittany Tawney's submission was refused by a verdict stored on 2026-08-19 about that same deliberate overhang — measured live at 0.12in for the tag and 1.56in for its cord. Carmen then trashed the design and spent forty minutes rebuilding six templates to satisfy a complaint that was never real. `template_audits.blocker_kind` (schema v15) records why a design is refused; `visual` clears a listing and everything else still stops it. The design thread's own recheck is unchanged and still refuses on a visible defect, because that is the thread where the question belongs. The reasoning is d613511's, unchanged: the finished flyer is inspected on its own render, which is the only check that sees what Gable actually did. |
| 2026-08-26 | A headshot well is portrait or square, never landscape | `_HEADSHOT_ASPECT` ran to 1.70 — wider than tall — and was wrong in both directions at once on the live designs. New Listing offered three candidates, its real portrait well at 0.62 plus both landscape wells of the secondary photo row, so Gable refused the design as ambiguous and told Carmen to delete two frames that are part of the layout. Open House was worse and silent: its true headshot well is 0.58, just under the old LOWER bound, so the only surviving candidate was a landscape photo well at 1.62 — Gable would have placed an agent's face in a property-photo slot, which is the exact failure `headshot_frames` exists to prevent, and nothing would have said so. Measured against all six live designs: 0.50 to 1.05 selects exactly one well, and the right one, on every design that has a headshot (0.58, 0.62, 0.65, 0.76, 0.77). |
| 2026-08-26 | An unchanged refusal is not repeated when the file is saved again | Carmen uploaded a .pptx at 12:47, was told it was not a Google Slides design, answered "I am working on it now", touched the file, and was told the identical sentence at 12:51. `quiet_when_clean` silenced only CLEAN re-reads, so a blocker re-posted on every Drive revision bump — which is exactly while somebody is working on it. The scan now records the new revision, so clearance stays truthful, and stays quiet when the verdict is unchanged apart from its new/updated timing word. Asking for a recheck explicitly still always answers, on a different path, so silence never becomes unresponsiveness. |
| 2026-08-26 | "Check them all again" is answered by a whole-folder sweep, not refused for want of a thread | Every recheck was keyed to a thread Gable already owned. Carmen asked "Gable, I just imported new templates. Can you check again?" at the top of the channel and got nothing; Chase asked the same thing with an @mention and got "I could not match this thread to a listing or template, so I have not changed anything" — while six designs sat unchecked. `recheck_catalog` re-measures the folder and answers once, naming what is ready and what still needs work. A person replacing several designs asks about the folder, because that is the unit of work they were doing. |
| 2026-08-26 | Removing a file Gable asked to have replaced is the fix, not a mistake | Carmen converted a .pptx, correctly deleted the original, asked Gable to check, and was told "I could not find the Open House.pptx design in Generic Templates. Put it back in that folder and ask me again" — Gable undoing its own instruction. A vanished file whose stored refusal was `unsupported` is now retired with an acknowledgement. A real design going missing still says put it back, because that genuinely is a problem. Same class as the credential remedy and the address ask: a right behaviour wearing a wrong sentence. |
| 2026-08-26 | A question asked outside a listing thread is answered with what is actually waiting | Chase replied "yes build it" in a channel thread and got a report on six templates, after six paid vision calls; he then asked "Gable is this now built?" and got "I could not match this thread to a listing, so I have not changed anything." Both were true and neither answered him. The cause was one tool doing two jobs: `rebuild_flyer` was the only route to a template sweep, so an unmatched build request swept the folder, and `report_status` with no matching run had nothing to say at all. `check_templates` is now its own tool, so a sweep happens when somebody asks for a sweep. `build_with_blank_fields`, `rebuild_flyer` and `report_status` with no run in the thread all answer with `context.waiting_summary`: which listings are paused and the exact question each is still owed, taken from the outbox that already stored it. Costs nothing and is the answer to all three questions. `waiting_store.py` was split from `question_store.py` in the same change, which is the honest seam -- that module is an outbox asking what Gable owes Slack, this one is a read asking what people still owe Gable. |
| 2026-08-26 | "Check again" in the thread of a design that has since been deleted answers about the folder | Carmen converted a .pptx, correctly deleted the original, and asked in that thread whether Gable could check again. The retirement fix stopped Gable demanding the file back, but the reply still only discussed one dead file and said nothing about the six designs she had just imported. A thread whose design has left the folder is spent, and the question asked in it is about the designs that ARE there. `_recheck` takes `sweep_when_gone`, set by the two design-thread entry points and not by the listing path, and a thread matching no design at all now sweeps rather than shrugging. `template_notify.py` was split from `template_triage.py` in the same change to stay under the ceiling: everything moved is about getting an already-decided sentence into Slack durably, and nothing moved decides what the sentence should be. |
| 2026-08-27 | **Reverses the 2026-08-14 row that recorded `p1_i90` as Client Review Post's hero well.** A testimonial carries no property photograph, and its one image well is the agent's face | Reported by Chase from the live channel while Porsher Howard's Client Review Post was stuck. Carmen answered "there is no property photo needed for this request", then "the image should be the headshot for Porsher, it's in the headshot folder"; Chase answered "no Gable use filed headshot". Gable asked "can you send me the image?" five times and the run sat in `needs_photo`, which nothing in Slack can release — a missing photo is deliberately not something `build_with_blank_fields` waives, and that is right for a design that really has a photo well. Measured through the service account against all six live designs the same day: every real property well is landscape and wide — New Listing 1.52 at 67% of the slide, New Listing with Open House 2.20 at 93%, Open House 1.68 at 74%, Sold 1.69 at 100%, Under Contract 1.60 at 100%. Client Review Post has exactly one image well, 5.55x9.49in, width-over-height **0.58**, portrait — inside the headshot band CLAUDE.md 4.3 item 13 measured, and the same 0.58 as Open House's real headshot. It was also the only one of the six where `headshot_frames` returned nothing, because the hero claim consumed its one portrait well before the headshot search could see it. So the second, quieter half of this defect: had a house photograph ever arrived, Gable would have deleted the agent's portrait well and placed the building there, with no face on the flyer and nothing said. `designs.NO_HERO_DESIGNS` now records the measurement, `find_hero_frame` returns None for those designs so the geometric search cannot re-invent the well (it would: 49% wide, top half, well over the area floor), the manifest drops `_HERO`, preflight stops calling a heroless design defective, and `runner` asks for a photo only when the manifest declares one — it had been unconditional while the build below already guarded its own hero work with `if hero_slot:`, so the ask and the build disagreed and the ask won. `place_headshot` now takes the design label; the comment that used to sit at that call had this exactly backwards, noting Client Review Post's hero was "narrow enough to matter" and concluding the search resolved it correctly. `pipeline/run_images.py` was split from `runner.py` in the same change to stay under the ceiling: it owns placing both photographs and deciding what counts as unfinished, and nothing that moved decides what to build. |
| 2026-08-27 | Gable claimed a capability it did not have, in the same thread, one message before failing at it | Same thread. Chase asked "this is a new skill for you without us giving you an image — are you able to do it?" and Gable answered "Yes, Chase. I can use Porsher's filed headshot from the Head Shots folder without a property image." It could not: the next message was "can you send me the image?" again. Two replies before that it had answered Carmen's headshot instruction with `tool=none` — pure acknowledgment, nothing recorded, run still parked — which `slackapp/brain.py` already names as "the worst outcome available" for a supplied value. No prompt change is made here, because the claim was not the cause: the design's own record was wrong and no honest answer was reachable from it. Recorded because the shape recurs — Gable is most confident about a capability at the moment its data says the work is impossible — and because a future prompt change should be measured against this exchange. |
| 2026-08-27 | The offer to reframe a flyer from a different photograph is made only by designs that have a photo well | Delivered flyers ended "send another photo here if you want it framed differently and I will redo it" regardless of design. On Porsher Howard's Client Review Post this arrived directly after five messages asking for a property photograph the design cannot hold, and it is not true there: the design's only image is the agent's face, which comes from the Head Shots folder rather than from a Slack upload. `run_reporting.reframe_offer` takes the same manifest answer the ask does, so the offer and the ask cannot disagree. |
| 2026-08-27 | A one-word name signing off at the END of a submission is the client; the same word at the top is not | Chase, reading Porsher Howard's thread: "if you see that title New Client Review Post can't you just build it without any input from Carmen?" For that listing the answer was yes and Gable asked anyway — row 130's `post_details` carried the whole quote and the client's name, " Sharon", on its last line. Read from the live workbook the same day: five Client Review Posts have ever been submitted, in five layouts, and `parse_review` read one of them. Both readable misses were the identical shape, a single capitalised word signing off at the end — " Sharon" on row 130, "Yvette" on row 39 — which `_NAME_LINE` cannot match because it requires two words. `_TRAILING_NAME` accepts one to three words, optionally dash-introduced, only on the last line of the quote paragraph, and only when a quote of at least `_MIN_QUOTE_CHARS` precedes it, so the closing sentence of an unsigned review can never be read as a signature. A single word deliberately still does not qualify at the TOP, where one capitalised word is far more likely to be a heading such as "Testimonial". Two corrections found by running it against the real rows rather than invented ones: the quote is taken from the longest paragraph rather than every line above the signature, or row 130 printed its "Five star review" header inside the testimonial; and a matched pair of wrapping quotation marks is stripped, because these designs draw their own quote glyphs and a pasted pair renders as a second set inside the first. Rows 49 and 50 still ask and should: their reviewer is a Google username, and "lucyglou" is not a name to print under a stranger's words about their own home. Three of five now build with no input from Carmen at all. |
| 2026-08-28 | Several images in one message are never discarded silently, and a repeated roster refusal reports the read it just did | Two threads Chase forwarded, both reading as "Gable is stuck". **Frank Lancelotta III:** Carmen sent three photos with "Here are 3 for the template." `carries_a_value` is "does the text contain a digit", so the bare 3 matched, the multi-file branch returned the answer-the-words sentinel, and all three uploads were dropped without a word. Gable then asked which of the three to use as the large photo — about files it no longer had — she answered "The road should be the large photo", and there was nothing for that answer to select. A new sentinel makes the caller say the images were not kept while still answering the words. **Halim Joseph:** the same refusal four times in identical words. Every one was TRUE — he reached Agents Contact Information at 13:48:48 and the last refusal went out at 13:47:16, so Carmen's edit had genuinely not landed — but she had said "I fixed that. Please run again." and got the same sentence back, could not tell a fresh read from a stored one, and gave up ninety-two seconds before it would have worked. The refusal now reports what the read returned ("40 agents"), which is the fact that tells her the edit has not saved. `agents/names.py` and `agents/profile_page.py` were split from `website.py` in the same change to stay under the ceiling: page mechanics fail on markup and network shapes, contact checks fail on whether a name and a phone agree well enough to print, and `clean_name` is shared so it sits under both. |
| 2026-08-28 | Gable places exactly two images and must never offer a design's smaller photo spots | Same Frank Lancelotta thread. Asked "The template has 3 photos, so please use all 3", Gable answered "I'll use the road as the large photo and the other two in the smaller photo spots." Carmen said "Perfect!" and waited. No such capability exists or has ever existed — `secondary` appears in the codebase only in comments about EXCLUDING those wells from headshot detection, and placement fills one hero and one headshot. The prompt's WHAT YOU CANNOT DO list covered choosing among photos but not filling extra wells, so the model had no reason not to offer it. Both limits are now written there, with this exchange named, because §2.1 is about the runtime agent as much as the building one: an offer nobody can fulfil costs more than an honest refusal. |
| 2026-08-28 | One address sentence, and it names the missing part instead of asking for the whole address back | Chase, reading Gable's own post-mortem: "Why are you asking for the address, it's already in the thread." It was — Gable had printed `3500 Hawks Hill Rd, Lot #1 & OR Lot # 2` twice in that thread itself. There were two address complaints in the codebase and only one of them had learned the 2026-08-19 lesson. `needs.incomplete_address` shows what is in hand and names what is missing; `manifest.validate` had its own wording, "I could not separate the street, city, state and ZIP confidently. What is the full address?", which asks for everything back and says nothing about which part is wrong. Frank Lancelotta III's listing got the good sentence at 11:55 and the vague one at 13:55. The sentence now lives in `listings/address.py`, which both callers already imported, and there is one of it. Found while fixing that: the "OR" joining the two lot numbers matched Oregon, so an address with no city, no state and no ZIP was reported as merely missing its ZIP. A state code that is also an ordinary English word now only counts where a state can sit — at the end — while an unambiguous code still counts anywhere, because a misplaced state is present and the ordering sentence is the honest one for it. |
| 2026-09-01 | A five-digit house number is not a ZIP, and one function counts ZIPs | Lina Mariner's Under Contract. The form gave `10600 partridge lane b3`; Carmen supplied `10600 Partridge Ln APT B3, Cockeysville, MD 21030`, and `manifest.names_one_property` counted two five-digit tokens and asked "which one is this post for?" three times while she said, three ways, that it was one condo. Earlier in the same thread `incomplete_address` had counted the house number as a ZIP and reported only the state missing. Both read the same `\b\d{5}\b` pattern from two private copies. `listings.address.zip_codes` is now the one reader and leaves the leading house number out — a range or a letter suffix included — and the manifest, the incomplete-address sentence and the research identity window all use it. A second five-digit house number mid-string still counts, because it belongs to a second property. `intake.address_looks_usable` keeps its permissive read on purpose: tightening it would put a second "I also need the address" beside the sentence that already names the missing part. |
| 2026-09-01 | A created image inside a frame Gable deleted inherits that frame's overhang | Brittney Bushee's Under Contract. The design's headshot well ran 19.74 points past the bottom of the page and its top sat under the "Under Contract" title band; `headshot_frames` clips a well clear of its neighbours, so the face was created 39.5 points lower and 19.7 points shorter than the well. The 2026-08-14 rule matched a created image to its frame only within two points on all four sides, so the clipped face matched nothing, was treated as appearing from nowhere, and the design's own overhang was reported as Gable's — "the agent photo runs about 20 points past the bottom edge of the page" — and parked the run. `layout._frame_it_replaced` now also accepts a source frame that CONTAINS the image, within the same two points, provided that frame is absent from the built copy, which is how a deleted well presents; the smallest such frame wins, so a background panel cannot stand in for the well. An image inside a frame cannot reach further off the page than the frame did. Measured against the built flyer and the live file. |
| 2026-09-01 | **Reverses the 2026-08-14 stop: a layout regression is delivered with what was measured, never held in review** | The same flyer was built, its link was in the database, and Carmen was told "I built the flyer, but the agent photo runs about 20 points past the bottom edge". She answered "That's ok. Please send it and I can adjust" and was refused, then sent to edit the source template — which she did, at 20:01, which is why the live file no longer shows the overhang. Chase's rules of 2026-08-17 (a flyer a check disliked goes out with what was noticed) and 2026-08-20 (Gable produces the flyer no matter what) had been applied to the vision pass and never to the geometric audit one step before it. The audit's sentence now rides under the link, the run event records the regression, and the vision pass keeps its layout-kind opinions whenever the audit found something. The measurement was right and useful; withholding a built flyer over it was the defect. What still holds a run is a flyer that does not exist or could not be read back. |
| 2026-09-01 | The official site's silence is retried once, logged, and then yields to a complete roster row and the brokerage credential | Brittney's recheck, 20:01:49 to 20:02:12. The profile read timed out once at its 20-second ceiling on a site that answered in half a second when tried from the droplet minutes later; its `except Exception` swallowed the cause without a log line, the memoised gate reused the failure for the credential phase, and the pause told Carmen to "correct the request or Agents Contact Information" — both right, as she said. Three changes. The read (now `agents/profile_lookup.py`, split from `website.py` at the 800-line ceiling) is tried once more after a transient failure — timeout, connection error, 5xx — with a jittered pause and a warning naming the exception type; a malformed or off-domain answer is not retried. `ProfileLookup.unavailable` tells the site's silence apart from an answer that names nobody. And when the roster row already proves name, email and phone and the site was wanted only for a credential, the silence is not evidence about the agent: the flyer prints `GABLE_DEFAULT_AGENT_CREDENTIAL` with `brokerage_default` provenance and the delivery message says the site was not reached. A site that ANSWERS "no such profile", a roster row with a blank, or an empty default still pause — now with a remedy that can work: run it again. Emptying the variable restores the old stop exactly, as the 2026-08-19 row promised. |
| 2026-09-01 | Both resume paths read the stated address from one function | Resuming Lina Mariner's run by hand after the deploy re-read the sheet's `10600 partridge lane b3`, not the `10600 Partridge Ln Apt B3, Cockeysville, MD 21030` Carmen had given in the thread and Gable had recorded, and asked for the whole address a fourth time. `store.load_submission` laid the stated address over the row for the Slack path; `tools/run_row.py --resume` built its submission from the sheet and never looked. The 2026-08-27 rule again — when two paths answer one question they read one source — so `store.stated_address` is the reader both use. |
